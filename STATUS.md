# MiG Alley — Linux port status

Last updated: 2026-08-28 (sprint 315)

## What works

- **3D flight, replay record and playback.** Quick missions and campaign missions fly; `.cam`
  replays record, save under a chosen name, and play back with multiple aircraft. PO-verified.
- **Tacview `.acmi` export** alongside every `.cam` (EPIC L). Emits real Lon/Lat, per-aircraft type
  names, player identification, IAS. PO-verified in Tacview: a 31 s one-on-one shows the F-86
  holding heading while the MiG-15 manoeuvres behind it — confirmed against the data (MiG behind in
  95% of samples; F-86 heading swing 16°, MiG's 360°).
  **Georeferenced (S305)**: the theatre is pinned to real Korea, so a track now overlays the map
  instead of floating ~900 km NE in the Sea of Japan. Gate: `port/acmi_georef.sh` (+ `CONTROL=1`).
  `port/acmi_regeoref.py` repairs recordings made before the fix without re-flying.
- **Front end**: menus, preferences, quick mission, campaign map, Player Log, replay dialog.
- Gates: `parity_2d` (2D screens vs gold-derived references), `map_drag`, ASan, stress.

## Open, with the next concrete step

| id | what | next step |
|---|---|---|
| PO-82-leak | 🔵 **S358: REFCOUNTING IS NOT THE FIX — the game never releases these surfaces at all** | **Measured before implementing, and the measurement killed the plan.** S349 found `Release()` is a no-op stub on all three surface classes and S350 quantified the leak (`Surface made=8002 freed=0`, `Surface2 7998/0`, growing linearly). The obvious fix was to make `Release()` real, using BoB's refcounted `SURF_Release` as the model. <br>**But `AddRef` is a stub too, so the game's own reference calls had never been counted either.** Instrumented both, freeing nothing: over a full sortie that reached 3D, **the entire game made ONE ref call — a single `Release()`, with ZERO `AddRef`s.** <br>**So refcounting cannot work here**: the game does not participate. A refcount starting at 1 would hit zero on that single `Release` and free a surface still in use — a use-after-free, which is exactly the outcome S349/S350 refused to risk blind. **Making `Release()` free would also fix nothing**, since it is called once per sortie against ~8000 leaked surfaces. <br>**The leak is not mis-freeing; it is ABANDONMENT.** The game creates surfaces and never releases them, which is very likely why the port's authors made `Release` a no-op in the first place — there was nothing to free against. <br>⭐ **S368: the ownership design, and the destructor bug that had to be fixed FIRST.** Creation sites located: `HARDWIN.CPP` makes only the primary/back buffers (a handful, and they must survive a flight); the volume is `WIN3D.CPP:4320/4462`, the TEXTURE path, which does `CreateSurface` then `QueryInterface(IID_IDirectDrawSurface2)` — two objects per texture, which is exactly why the census reads `Surface 8002 / Surface2 7998`, near 1:1. So ~4000 textures a sortie, and their natural owner is the **3D session**. <br>**Design:** tag surfaces created with `DDSCAPS_TEXTURE` (the discriminator that spares the primary/back buffers), and sweep them at the teardown point `MIG.CPP:2482`, immediately after `Launch3d` returns. That bounds the leak to one sortie instead of one session — memory comes back between flights. <br>**Hazard 1 is already covered:** `~IDirectDrawSurface` calls `ma_d3d_texture_forget(this)`, so freeing a surface clears the S348 handle registry rather than leaving the dangling pointers S348 existed to remove. <br>⭐ **Hazard 2 was real and is now FIXED (S368).** The destructor freed `sbits` but ABANDONED the `sview` twin — an `IDirectDrawSurface2` that borrows those same pixels and is exclusively owned by the parent. Nothing deletes surfaces today, so it was latent; the first thing a teardown sweep would have done is turn it into a live object holding freed pixels, i.e. the use-after-free S349/S350 refused to risk blind. `sfreeview()` now disposes of the twin, defined in `d3d_execbuf.h` where `IDirectDrawSurface2` is complete (deleting through the forward declaration would be undefined behaviour). Builds clean; **not yet exercised, because nothing frees surfaces until the sweep exists.** <br>⭐ **S369: the sweep was BUILT, RUN, and the hypothesis is REFUTED — with the real owner named.** `MA_FREE_TEX_SURFACES=1` tracks `DDSCAPS_TEXTURE` surfaces and frees them at `MIG.CPP` after `Launch3d` returns. It works mechanically — census went `made=1125 freed=877` and `[surfsweep] freed 1122 texture surface(s)` — and then **SIGSEGV immediately**, in the teardown path itself. Backtrace resolved: `direct_3d::RenderTileToDDSurface` → `IDirectDrawSurface2::Blt` → `IDirectDrawSurface::salloc()` **on a freed surface**. <br>**So texture surfaces are NOT owned by the 3D session.** The game renders terrain tiles into them AFTER the flight ends; they belong to the tile cache, which outlives a sortie. The session boundary is the wrong ownership boundary, and this is the use-after-free S349/S350 declined to risk blind — caught behind a default-off flag on a gate sortie instead of in the PO's session. <br>**Also corrected here:** S368 claimed the S348 registry hazard was 'already covered' because `~IDirectDrawSurface` calls `ma_d3d_texture_forget(this)`. It is not: the registry stores the **Surface2** pointer and the forget matches on it, so passing the parent never matches. The clear now lives in `~IDirectDrawSurface2`. <br>⭐ **S370: the replacement theory is REFUTED TOO — measured at zero.** S369 pointed at `ma_d3d_texture_register`, which overwrites `texmap()[handle]` silently; the theory was that `RemakeTexture` re-registers a handle with a fresh surface and abandons the previous one, which would make the superseded surface unreachable and safe to free. Implemented and run: **zero events over 1002 surfaces** — not one handle was ever re-registered with a different `Surface2`. The game does not remake textures in place; **every surface gets its own fresh handle.** No crash either, because the code could never fire. Removed rather than left switched off — an inert knob reads as a fix that is merely disabled. <br>**So the leak is ~8000 DISTINCT live handles per sortie, all still reachable through the registry**, which is why neither a session sweep (they are still in use — S369's segfault) nor a replacement free (there are no replacements) can touch it. The remaining question is what makes the handle COUNT unbounded: whether the game re-creates textures for the same art each sortie without reusing handles, or genuinely needs ~8000. `s_texMaxHandle` across two consecutive sorties in one process answers it. **Four sprints spent on this item; rotating off.** <br>**Next sprint:** ownership per TILE-CACHE entry rather than per session — free a texture surface when the tile that owns it is evicted or replaced — or keep the sweep and move it behind the tile cache's own teardown. The flag stays OFF. <br>**So the fix must be OWNERSHIP-BASED, not reference-based**: the port tracking surfaces and freeing them when their owner dies (scene change, mode change, dialog teardown), or pooling and reusing them. That is a design change and needs its own item. <br>⚠️ The white textures themselves are already fixed (S348, 820k failed lookups → 0); this is the memory leak underneath, not the PO's visible symptom. |
| gates | ✅ **S357: the suite is 23/24 — `real_mouse`/`real_hover` were MY contention, and the nesting guard is now verified** | **Both pointer gates re-run on an IDLE machine: `real_mouse -> PASS (36s)`, `real_hover -> PASS (36s)`.** They failed in the S352 suite run because I was running JuliaRacer's smoke tests concurrently on the same 4 cores — exactly the interference `CONCURRENCY.md` and `gl-lock` exist to prevent, and exactly the trap of manufacturing a port defect out of load. **These two gates drive the REAL pointer and are the most contention-sensitive in the suite**; nothing heavy may run beside them. <br>**So the suite's true state is 23/24**, the single failure being `parity_2d`, whose residual is a stale oracle awaiting the PO (see the row below) rather than a defect. <br>⭐ **S352's nesting guard is now BEHAVIOURALLY verified, not just syntax-checked.** `gl-lock bash port/gates_all.sh real_mouse` — the exact invocation that used to hang forever — now prints `### already inside gl-lock (held by pid N, an ancestor) -- running without re-acquiring` and completes `1/1 clean`. |
| parity_2d | 🟡 **FIXED 88% (S354); the residual is a STALE ORACLE — needs the PO to confirm gold** | **The remaining 1497 px are three tab labels we now draw and the reference does not.** The control is `IDC_RRADIO_F86VARIANTS1` (id 1050) and it is a populated tab bar — traced: `AddButton("Scenario")`, `AddButton("UN")`, `AddButton("Red")`, `OnDraw items=3 cursel=0 bounds=(0,0)-(462,26)`. <br>**The oracle predates the control being drawable.** `port/ref/native/README.md` dates `quickmission.png` to **2026-08-16 (S143)**; **PO-83 — which ADDED the `CT_RADIO` draw branch, previously missing entirely — landed 2026-08-29**, thirteen days later. When the oracle was captured this tab bar could not draw at all, so the reference records a screen with a control MISSING. <br>⚠️ **NOT re-captured, deliberately.** Re-baselining an oracle to make a gate green is the same fault as a test that asserts the bug (BoB R1/R13, JuliaRacer `contact_smoke` this session), and the fact that our output *looks more correct* is not proof. **The question only the PO can settle: does the original game show a `Scenario | UN | Red` tab bar on the Quick Mission screen?** If yes, the oracle is stale and should be re-captured **now that S354 is in** (before S354 a re-capture would have baked the black box in permanently). If no, the control should not be drawn here and the fix belongs in visibility, not in the reference. <br>**Everything else is byte-identical**: title, prefs_3d, prefs_others, campaign_map — so this is one control on one screen, not a broad regression. |
| PO-87 | ✅ **DONE — `port/TACVIEW.md`** | **PO 2026-08-30**: *"add backlog items to ma and bob - include the instructions on how to install tacview in the ma and bob repo"*. <br>This port EXPORTS `.acmi` for Tacview and the PO has been loading those files by hand, but nothing in the repo says how to get Tacview onto this machine or where the exports land. <br>**Cross-port twin of BoB's R15** — same instructions, two repos; write it once and note any REAL difference (export path, ACMI version, the copy-to-Tacview step) rather than duplicating blindly. <br>**Should cover:** obtaining Tacview on Linux (which build, native or under Wine), installing it, where this port writes `.acmi`, how to load one, and the caveats already established here so a reader is not re-deriving them — above all **S276/S327: Tacview orients the model from `Yaw` ALONE, in the OPPOSITE rotational sense from compass heading**, which cost this port two sessions and a wrong "aircraft flying backwards" diagnosis before it was proven with a four-variant synthetic control. <br>**DELIVERED (2026-08-30): `port/TACVIEW.md`**, written from what is actually on this machine rather than generically: the self-contained `sgl/SAT/tacview/tacview.sh` launcher (own win64 prefix, installs Wine Mono first because Tacview's addons need .NET, picks the newest installer by `sort -V`), the second independent 1.9.5 prefix behind `~/Desktop/Tacview.desktop`, and the exact export path `…/rowan/mig/acmi_current.txt` — **fixed name, so every sortie overwrites the last**, which is how an earlier session lost a control arm's evidence. Carries both caveats a reader would otherwise re-derive: **S276/S327 — Tacview orients from `Yaw` ALONE, opposite sense from compass**, which produced a wrong "flying backwards" diagnosis here; and BoB's R10 truncation for comparison. Also points at `tacview_export.sh` / `acmi_orientation.sh` for checking an export without opening Tacview. |
| PO-67 | Front end laid out at 800x600 on a 1920x1080 display; ~77% black | ✅ **FIXED AND FLIPPED (S325).** `MA_MAXIMIZE` is **ON by default**: the front end fills the screen. **Maximised interaction suite 12/12** (was 1/10 at S318), **`parity_2d` 5/5 byte-identical** with the default flipped, `sysbox_exit` — the gate that reverted S319 — **green**. ⭐ **The blocker was a handler that was CORRECT and UNREACHABLE.** `class RMdlDlg : public CDialog` — *not* `RDialog` — so it never inherits `RDialog::OnRowanMessage`, the virtual that dispatches `WM_GET*` on this port; `ON_MESSAGE` is a no-op macro in compat, so `RMdlDlg::OnGetGlobalFont` (which returns the small font set, and always did) was **never called**. The modal's controls asked, got 0, `SelectObject(NULL)` changed nothing, and the DC kept the panel's **h=40** font → text overflowing a 279x142 dialog, YES/CANCEL overlapping, and the click landing on Save. Fixed by giving `RMdlDlg` the dispatch it never had (~15 lines, adds reachability only). **Four earlier hypotheses were each killed by measurement**, and every one would have been a change to a byte-identical reference layout: rects-follow-font (S320), derived base units (S321, refuted — 6x13 is right because Windows maps DLUs with the TEMPLATE font), font misrouted to the panel (S322, refuted by Wine — dialogs stay SMALL at 1080), controls wrongly parented (S324, refuted — `hosted=5`). **Third flip attempt:** S312 flipped on green gates and shipped a dead front end (the PO found it); S319 flipped and its own new gate reverted it; S325 flips with the blocker fixed. The difference is not confidence — `port/maximized_nav.sh` is in the default suite and has **demonstrated it can FAIL** (`MA_NO_DRAWORG=1`), so the S312 failure mode (nothing in the suite could contradict the change) no longer exists. `MA_MAXIMIZE=0` reverts. |
| PO-72 | Campaign instruction / next-mission text missing after 3D exit | **Blocked on the PO** — a screenshot decides whether the text is ABSENT or drawn BLANK/ELSEWHERE. Those need opposite fixes. |
| S248 | ~~`parity_2d` campaign_map is RED (5184 px)~~ | **CLOSED (S315). `parity_2d` is now 5/5 byte-identical** — campaign_map was the last red screen. Cause: `CRToolBar::OnGetFile`'s art guard, widened from `0x6800..0x7100` to `0..0xFFFF`, admits two files the real game does not draw — **`0x6607` and `0x660c`** (two art files, three cells: the star appears twice, at 48x48 and 24x24). The 0x66xx range is re-excluded; the rest of the widening is kept. `MA_WIDE_TBART_66=1` reverts. |
| PO-74 | Campaign buttons wrong | **S319: the PO's white rectangle is REPRODUCED and its state is now known** — evidence in `port/ref/po74/`. It appears at campaign date **7/3/50, Morning/planning, with the MISSION 2 BRIEFING and D.I.S. panels open**, and NOT on the freshly-loaded map: `toolbar_clean_start.jpg` (t=16s, same session) shows both toolbar rows complete, while `toolbar_white_7-3-50_mission2.jpg` (t=408s) shows the **top row blank white across x≈680..1030 with a black gap to x≈1200**, and the whole map/clock/!/star/clipboard/compass group **shifted DOWN one row**. So the toolbar gains a row in this state and the new row's background paints white where no button art covers it. That is why S316's 6/25/50 capture looked clean — it never entered the state, exactly as S316's own caveat said. **The second half is separate and also visible in BOTH frames: a flat grey button bearing only a faint star**, sitting among buttons that do have art — a missing/placeholder icon, not the white rectangle. S315 excluded `0x6607`/`0x660c` for parity; check whether the star is one of them before assuming the two are the same defect. ◐ **DRIVEN AND CAPTURED (2026-08-29) — THE ARTEFACT IS NOT PRESENT, WHICH IS CONSISTENT.** `MA_CAMP_FLY=1 MA_CAMP_LOOP=1 MA_CAMP_LOOP_SHOT=1` flew a campaign mission and captured the returned map at 1920x1080 (`[shot] campaign loop complete`, 98 % non-black, toolbars present — 117–226 distinct colours per 30-row band across the top, so this is a real populated screen and not a blank). **Scanned the top 200 rows for runs of near-white pixels: the longest is under 100 px.** No white rectangle. <br>⚠️ **That is EXPECTED and it narrows the repro, rather than contradicting S319.** S319 says the artefact needs the **mission-2 briefing AND D.I.S. panels OPEN** — this recipe returns to the plain map and captures there, so it reproduces the CLEAN state, exactly as `toolbar_clean_start.jpg` does. The missing ingredient is opening those panels, not flying further. <br>⭐ **THE D.I.S. PANEL HAS A CLICKABLE ID (2026-08-29): `IDC_DIS = 2072`**, wired as `ON_EVENT(CMainToolbar, IDC_DIS, 1 /*Clicked*/, OnClickedDis)` (`MAINTBAR.CPP:153`); directives is `IDC_DIRECTIVES = 2074` / `OnClickedDirectives`. `BOB_CLICKSEQ` takes the `#ID` form, so the panels can be opened by recipe — the state S319 identified needs **the mission-2 briefing AND the D.I.S. panel open**, and the campaign-loop capture (which reproduces only the clean map) can be extended with `#2072` rather than needing the PO's save. <br>⚠️ Getting the ORDER right matters: S319's evidence has both panels open at 7/3/50 together. Opening one and capturing would be a third distinct state, not the reported one. <br>⭐ **S360: THE WHITE BLOCK IS THREE TOOLBAR BUTTONS — and its mechanism is PO-82's, which is now FIXED.**
Measured from S319's own evidence instead of re-reproducing the state. In
`toolbar_white_7-3-50_mission2.jpg` the artefact is a **solid near-white rectangle, 526 × 41 px at
(589,0)** in a 1950×105 toolbar crop — i.e. spanning 30%–57% of the strip. `IDD_MAINTOOLBAR` is
353 DLU wide (`ENGLISH/MIG.RC`), so that band is **DLU ~107–202**, covering `IDC_DIS` (118–150),
`IDC_AUTHORISE` (150–182), and the edges of `IDC_WEATHER` (86–118) and `IDC_DIRECTIVES` (188–220).
**Three adjacent buttons drawn as one white block** — which is precisely this item's title, "Campaign
buttons wrong".
<br>⚠️ **RETRACTED (S361): the PO-82 mechanism does NOT apply here.** S360 argued a white control is an
untextured one and therefore the same bug as PO-82, fixed by S348. **Checked, and it is wrong:** the
toolbar is drawn by `ma_ole_draw_toolbar` → `ma_button_draw(..., screenHdc, ...)`, the **GDI path onto
the screen canvas**. These buttons never reach `ma_tex_desc` or the texture registry S348 repaired, so
that fix cannot touch them. The resemblance was real and the inference was not.
<br>**Also eliminated:** a wrong icon mapping. `ma_button_apply_icon` carries a hardcoded id→FileNum
table, and all four controls in the band match `RESOURCE.H` exactly — `IDC_WEATHER 2069`,
`IDC_DIS 2072`, `IDC_AUTHORISE 2023`, `IDC_DIRECTIVES 2074`.
<br>⚠️ **CALIBRATION CAVEAT (S362), which weakens S360's naming of the controls.** The px→DLU mapping
assumed the 1950×105 crop spans exactly the toolbar's 353 DLU width. That cannot be checked from the
evidence: the full screenshot is **960×540** (half-scale of 1920×1080) while the crops are **1950 px
wide — wider than the screen itself**, so they are upscaled by an unknown tool and their framing is
unverified. (The non-uniform px/DLU between x and y is *not* itself an error — Windows DLU x and y use
different base units by design — but it removes the easy consistency check.) **So "the block covers
IDC_DIS and IDC_AUTHORISE" is PROBABLE, not established.** What survives without the assumption is
only proportional: a solid filled rectangle occupying ~27% of the strip's width, starting ~30% across.
<br>⭐ **AND IT IS NOT THREE BLANK BUTTONS.** `IDC_AUTHORISE` ends at DLU 182 and `IDC_DIRECTIVES`
starts at 188; that 6-DLU gap maps to px 1005–1038, which lies **inside** the solid 589–1115 white run.
The block covers the buttons *and the toolbar background between them*, so the failing thing is the
strip itself — a background/fill, not per-button art. That is a different search from S360's.
<br>⚠️ **Not claimed as fixed** — the state still has to be reached to confirm it, and reaching 7/3/50
with the mission-2 briefing open is the same campaign-state obstacle S359 left. But the question has
changed from *"what draws this?"* to *"is it still there?"*, which is a much cheaper question.
<br>⭐ **S359: the D.I.S. panel ALONE does NOT produce the artefact — one condition eliminated, with the click PROVEN to have landed.** Drove the campaign map on the pinned pristine save and clicked `#2072`. Added `MA_TRACE_DIS` first, because a capture with no white rectangle is meaningless unless the click is known to have fired — *"the panel never opened"* and *"the panel opened and the artefact is absent"* are opposite findings from an identical screenshot. Result: `[dis] OnClickedDis fired`, and the capture has **7554 white px (0.36%) with a longest horizontal run of 17 px** — scattered text, not a rectangle. <br>**So the artefact needs more than D.I.S. being open**, which supports S319's claim that the mission-2 briefing must be open *too*, at 7/3/50. <br>**Next: reach 7/3/50 with the mission-2 briefing open.** The pinned `campaign_pristine.sav` is not at that date, so this needs either a save advanced to it or a recipe that plays forward — that is the remaining obstacle, and it is about campaign STATE, not about knowing which control to click. <br>(The player's `Auto Save.sav` was stashed and restored around the run; verified byte-identical.) |
| PO-75 | Pre- and post-mission campaign messages wrong | **PO 2026-08-27**, same video. The briefing/debrief text around a campaign mission does not match. Distinct from PO-72 (campaign instruction text missing *after 3D exit*), which is about text being absent; this is about the wrong text being shown. Check whether fixing one resolves the other. **S318 (2026-08-28): one concrete defect found in the video — in-flight message text loses its SPACES.** At 3x zoom the tower call reads `Tower: ViperLead,Windat35,000ft,bearing115,100knots.Gusting:light.` and the reply `ToViper2: wearerolling.`; the radio menu shows `NextWaypoint` / `MessageSubject` / `MissionIP`. Letters are spaced correctly — only the space collapses to ~1px against ~5-7px letters. Every glyph's advance is one byte in `SRC/3D/OVERLAY.CPP:PutC3` — `pWidth = pmap->body[uChar & 0x7F]; ... x += pWidth;` — read from the font image map, while the `bigWidths[]` table in that same file says space should be **8**, as wide as a capital. `MA_TRACE_FONTW=1` (new, built) dumps `body[]`/`alpha[]`/`bigWidths[]` per character so the two can be compared directly. ⭐ **S371: ESTABLISHED AS A PORT DEFECT, AND FIXED.** The probe answered it. Measured on a hardware sortie: the port gives space an advance of **2 px** while letters get 5-6, so `body[' ']=3` against `body['o']=6`. The engine's OWN table says the opposite — `bigWidths[' ']=8` against `bigWidths['o']=7`, i.e. a space is WIDER than a letter. Port 0.5x, original 1.14x: a **2.3x relative under-advance**, which is precisely what runs words together. <br>**Cause:** these widths no longer come from Rowan's font map at all — the port synthesises them from a substituted TrueType face via `ma_gdi_glyph_gray8`/stb_truetype, whose space advance at this pixel height is 2. `git log -L` shows the advance line itself is untouched 2015 code, so the code is innocent and the DATA changed underneath it. <br>**Fix:** for glyphs with an EMPTY bitmap only, take the engine's own `bigWidths` metric. Whitespace contributes no pixels, only advance, so no rendered glyph can move — verified: exactly ONE character (32) is affected, and `o`/`m`/`i` keep their previous widths. `body[' ']` now 8, was 3. `MA_NO_SPACEFIX=1` reverts. <br>~~NOT yet established that this is a PORT defect~~ ⚠️ **superseded — it is one** rather than how the original renders — the probe decides that, and it needs a display. Do not 'fix' the advance before comparing against gold. |
| PO-76 | **EPIC: get multiplayer working** | **PO 2026-08-28.** The game's OWN multiplayer code is present and compiled — this is a BACKEND gap, not a feature to write. `class DPlay` (`SRC/H/WINMOVE.H:591`, `StartCommsSession()` at :879) is implemented in `SRC/COMMS/COMMS.CPP` and built into the `_COMM` unity: `H2H_Player[MAXPLAYERS]` (:121), `ExitDirectPlay` (:166), `UIMultiPlayInit` (:209), `UISelectServiceProvider` (:231). The front end is wired too — `FULLPANE.CPP:275` `{IDS_TITLE3,&selectservice,&RFullPanelDial::StartComms}` → `StartComms` (:3761) → `if (_DPlay.StartCommsSession()) return TRUE;` else the `IDS_NOTCONNECTED` box. ⚠️ **CORRECTED 2026-08-28 (S323): my first statement of the gap was wrong.** I wrote "nothing defines `DirectPlayCreate`" — true, but IRRELEVANT: **the game never calls it.** `DPlay::CreateDPlayInterface()` (`SRC/COMMS/Comms.cpp:1408`) constructs the object through **COM**: `CoCreateInstance(CLSID_DirectPlay, NULL, CLSCTX_INPROC_SERVER, IID_IDirectPlay4A, &lpDP4)`. And compat's `CoCreateInstance` (`SRC/compat/objbase.h:183`) is a blanket stub — `*ppv = NULL; return E_NOINTERFACE;` for EVERY CLSID. So the chain is `CoCreateInstance → E_NOINTERFACE` ⇒ `CreateDPlayInterface() FALSE` ⇒ `UIMultiPlayInit() FALSE` ⇒ `StartCommsSession() FALSE` ⇒ the `IDS_NOTCONNECTED` box. **The single entry point to implement is `CoCreateInstance(CLSID_DirectPlay)` returning a socket-backed `IDirectPlay4A`** — the vendored `dplay.h` interface is then exactly the vtable to fill. ⭐ **BoB is IDENTICAL** (`SRC/COMMS/Comms.cpp:807`, same call, same `IID_IDirectPlay4A`, same stub), which strengthens the write-once case. 🎁 **S372: BoB HAS ALREADY BUILT IT — adopt, do not rewrite** (note 43, `port/CROSS-PORT-FROM-BOB-2026-08-30-note43.md`). `~/bob/SRC/compat/bob_dplay.cpp` is a **567-line DirectPlay-over-UDP shim** (R6.1 object, R6.2 transport) answering the same `CoCreateInstance(CLSID_DirectPlay)` call, in the same engine, against the same vendored `DPLAY.H`. Both BoB gates measured **green today**: `bob_mp_connect` reaches the lobby *with a negative control*, and `bob_mp_packet` does discovery, join and a packet across two processes (`RECEIVED 22 bytes from pid 2`). **So sprint 1 is to PORT that file and wire MA's `CoCreateInstance`, not to implement 53 methods.** The vtable is identical since both vendor the same header; expect the work in the compat glue (`objbase.h`'s blanket stub, and whatever MA's `winsock.h` does or does not provide). ⚠️ Note 43 also corrects the chain below: the shim must answer **`CLSID_DirectPlayLobby` too**, which the game asks for FIRST. <br>⭐ **S370: PREMISE CONFIRMED BY MEASUREMENT, and the cost is now known.** `MA_TRACE_COM=1` on a headless Multi-Player click shows exactly two COM requests, both refused: `CLSID_DirectPlayLobby` (`2fe8f810`, `DPLOBBY.H:85`) and then **`CLSID_DirectPlay` (`d1eb6d20`)** → `E_NOINTERFACE`. So the documented chain is real and not merely plausible — an unimplemented path and a path that is never reached look identical from the outside, and this distinguishes them. **A shim must answer BOTH CLSIDs**, which the earlier write-up did not mention. <br>⭐ **Cost shape.** The vendored DX6 `SRC/H/DPLAY.H` declares the interface with `DECLARE_INTERFACE_`, and this port's `objbase.h` defines `PURE` as `= 0` — so every method is pure virtual and a concrete class must define **all 53 methods of `IDirectPlay4`** (DP2 declares 32, DP3 47, DP4 53; the derived interface redeclares the inherited ones). Of those, the game calls exactly **17**: `EnumConnections`, `InitializeConnection`, `GetCaps`, `Open`, `Close`, `EnumSessions`, `CreatePlayer`, `DestroyPlayer`, `CreateGroup`, `AddPlayerToGroup`, `DeletePlayerFromGroup`, `GetSessionDesc`, `SetSessionDesc`, `Send`, `SendEx`, `Receive`, `Release`. The other 36 can return `DPERR_UNSUPPORTED`, but they must exist or the class cannot be instantiated. <br>**Approach:** implement the DX6 `IDirectPlay4` vtable over UDP/TCP sockets in compat (enumerate one "Internet TCP/IP" service provider, host/join a session, `Send`/`Receive` the game's own packets) — the same shape as the DirectInput→SDL and DirectSound→OpenAL shims already here. ⭐ **CROSS-PORT: write it ONCE.** BoB is the same Rowan engine with the same `DPlay` class and the same `Aggrgtor` packet layer (bob R6). Whichever port implements the shim first, the other adopts it — as with RLE8 decode and the D3D7 refcount fix. Keep `port/BOB_PORT_LESSONS.md` == `~/bob/doc/ROWAN_ENGINE_LINUX_PORT_NOTES.md` in sync. **Sprint 1 = a connectivity gate BEFORE any UI work:** two processes on localhost reach `StartCommsSession()==TRUE` and exchange one packet. **Done when** two instances play a mission together, with the packet rate and any desync measured and recorded, not judged by feel. |
| PO-77 | **Replay SAVE screen: the file list display is corrupted** | **PO 2026-08-28**, screenshot. After saving a replay, the save screen's file list (`ma-1v1-long`, `ma-long`, `corpus-baseline`, … `Current File` + edit box) is drawn **over the film-strip background art with the art still showing through and around it** — overlapping frames, no cleared backing, list text bleeding across the thumbnails. This is the panel→panel stale-pixel family: `ma_gdi_clear_screen()` is called on map→panel (`_wasMap`) and 3D→panel (`_was3d`), and the S155 comment gives the reason (*"wherever the panel does not cover, the stale frame shows through"*). **Save→list is another transition with no clear.** ⚠️ Note the list and the `Current File` edit OVERLAP by design (S210: the edit at (14,187) 202x26 sits wholly inside the list rect (10,128) 294x260) — so any fix must not re-break the S210 z-order (topmost gets first refusal), and `MA_NO_EDIT_FIRST=1` reverts that. Video `/home/admin/Videos/260828_ma.mp4`. <br>◐ **CONFIRMED THERE IS NO PANEL→PANEL CLEAR (2026-08-29):** `ma_gdi_clear_screen()` has exactly TWO call sites in the whole tree, `MIG.CPP:1741` (`_was3d`, 3D→panel) and `MIG.CPP:1766` (`_wasMap`, map→panel). A panel→panel transition clears nothing. <br>⚠️ **S374 CORRECTION: the S367 panel→panel clear does NOT cover this screen.** Measured and then confirmed in source. Driving to the Replay screen shows exactly one transition (`art 28161 -> 28167`), and four further click candidates on it produce **no further transition at all** — the art number never moves. The reason is at `FULLPANE.CPP:3305`: the save screen is raised by `LaunchDial(new CLoad(IDD_SAVEREPLAY, LSD_SAVE, FIL_VIDEOS_START_DIR, ...))` — a **DIALOG launched over the panel**, not a panel page change. S367 keys the clear on the panel's artwork changing, so it cannot fire here. S367 fixed a real gap (panel→panel genuinely had no clear, and parity is unmoved by it) but it is **not** the fix for what the PO photographed, and recording it as one would have closed this item on the wrong evidence. <br>**So PO-77 is a DIALOG-over-panel problem.** Candidate (b) — the list host not erasing its own rect — is already fixed selectively via `ma_lb_force_fill` for exactly this `CLoad` list (measured 75,456 px of film-strip art coming through a 294-px-wide column). Candidate (a) needs restating: art showing *around* a dialog is normal unless what is beneath it is STALE. **Next: reach the dialog and look** — the `rN` row clicks do not trigger it, so the recipe needs the control that calls `OnSaveReplay`. <br>~~⚠️ BUT THAT IS ONE OF TWO CANDIDATES, AND THEY NEED DIFFERENT FIXES.~~ (a) the SCREEN is not cleared across the transition, so the previous panel's art shows through where the new one does not cover; (b) the LISTBOX HOST does not erase its own rect before drawing rows, so old content bleeds inside the list. The PO's words — *"the art still showing through and around it, overlapping frames, list text bleeding across the thumbnails"* — fit BOTH: "around it" suggests (a), "bleeding across the thumbnails" suggests (b). Do not pick one by reading the sentence again. <br>**The existing instrument answers it:** `MA_TRACE_PAGE=1` already prints `currentpage` and the panel pointer `fp` each idle (`MIG.CPP:1751`). Drive the replay save flow with it and watch whether `fp` CHANGES at the moment the list appears. If it changes → (a), a missing transition clear. If it does not → the panel is redrawing in place and the corruption is (b), inside the list host. <br>⭐ **STRONGER CANDIDATE (c) — THE OPAQUE BACKING FILL IS DISABLED FOR EVERY LISTBOX (2026-08-29).** `RLISTBXC.CPP:518`: <br>`if (getenv("MA_LB_FILL") && !ma_oob_lb_draw && (!artnum && m_FirstSweep != TRUE \|\| !m_hWnd))` <br>The fill is gated behind an env that **defaults OFF**, so no listbox paints an opaque box any more. S70/S71 removed it deliberately — gold shows the sky through the prefs tab strip and translucency in the OOB Player Log tables, and the note records that the title menu "matches gold for the first time" without it. **But that is exactly "the file list drawn with the background art showing through"**, which is what the PO reports for the replay save screen. <br>⚠️ **THE CODE AND ITS OWN COMMENT DISAGREE.** `ma_olecontrol.cpp:41` still says *"the front-end menu/prefs listboxes never set it, so they keep the opaque box they rely on"* — describing behaviour the `MA_LB_FILL` gate removed. Anyone reasoning from that comment (as this entry's first two candidates implicitly did) will look in the wrong place. <br>☑ **FIXED (2026-08-29), selectively.** `ma_lb_force_fill` is set around the file list's `OnDraw` in `ma_ole_draw_all` (the panel loop), so control **1055** — measured as `rect=(10,128 294x260)`, matching S210's note exactly — regains the opaque box while every other listbox keeps the S70/S71 behaviour. `MA_LB_FILL_IDS` overrides the id set; `MA_LB_FILL=1` still forces it everywhere; `MA_NO_LB_FILL=1` suppresses it.

| check | before | after |
|---|---|---|
| `parity_2d` (global restore) | 4 of 5 screens moved — title 7884, prefs_3d 18371, prefs_others 18219, quickmission 6826 | — |
| `parity_2d` (selective) | — | **5/5 byte-identical** |
| replay A/B, `MA_LB_FILL` off vs on | 75,456 px | **9,210 px** |

The 88 % drop is the file list now filling by default in BOTH arms; the residual 9,210 px is the title-menu listbox (105x100 = 10,500 px), which by design keeps no fill and which `parity_2d` holds byte-identical.

⚠️ **WHAT IS AND IS NOT PROVEN.** Proven: the opaque box is restored for the file list and nothing else moves. NOT proven: that the PO's screen now looks right — **there is no gold capture of the replay screen anywhere in `port/ref/`**, so "correct" here rests on the PO's report that art showing through is wrong, not on a reference. Capturing that screen under Wine would close the gap and is worth a sprint.

⚠️ **THREE DRAW PATHS, TWO OF THEM INERT.** `CRListBoxCtrl::OnDraw` has three call sites; the file list uses only `ma_ole_draw_all`. Hooks in `ma_ole_draw` and the `ma_oob_lb_draw` block were written first and did nothing (`MA_TRACE_LBID` silent from both) while `parity_2d` passed the whole time — a flag that never fires breaks nothing, so the gate could not distinguish "fixed" from "inert". Only the A/B number could. One of those attempts also **reported success while writing nothing**, because a stale line number pointed past an earlier insertion; caught by grepping the BINARY for the trace string, which is the check that catches an edit that did not land.

_Original test plan:_ **Test, cheaply and first:** re-run the replay save screen with `MA_LB_FILL=1`. If the corruption disappears, the cause is the missing fill, not a missing screen clear — and the fix must be SELECTIVE (restore the box for the lists that need it) because a blanket restore re-breaks the prefs tabs and title menu that now match gold. `parity_2d` holds those five screens byte-identical and will catch it. <br>⚠️ **A blanket "clear on every panel change" is the obvious fix and the risky one:** `parity_2d` holds five screens byte-identical, so any added clear must be verified against it, and shipped with a revert env like every other clear in this file. |
| PO-78 | **Replay: CTRL+F6 does not show bogie view** | **PO 2026-08-28.** In replay, CTRL+F6 should show the bogie (target) view; instead the F-86 makes a **quick twitch motion** and the view does not change. The twitch says the key IS reaching the game and doing *something* — so this is a mis-bound or half-implemented action, not a dead key. Start at the keymap: `commonkeymaps` / `KeyMap3d` and the DIK+shiftstate path (`Reg3dConv`, which R1.3b had to bounds-fix), and compare CTRL+F6's entry against plain F6. `MA_TRACE_KEY=1` reports actions as they fire. Video `/home/admin/Videos/260828_ma.mp4`. <br>⭐ **ROOT CAUSE IDENTIFIED (2026-08-29) — the binding is fine; the view is STOLEN BACK.** The key path was proven good twice already: S239 added `Ctrl+F6 → OUTREVLOCKTOG` (GNOME claims `Alt+F6` for cycle-group), and S240 un-guarded the reverse padlock, which Rowan had compiled out of release builds behind `#ifndef NDEBUG`. What remains is `Viewsel.cpp:2051`: <br>`case VM_OutPadlock: case VM_Track: case VM_OutRevPadlock: ... viewnum.viewmode = VM_Track;` <br>`InitFlyingView` **resets the mode**, so the padlock establishes its view, runs `InitOutRevPadlock`, draws ONE frame, and is taken back. **That single frame is the PO's "quick twitch motion"** — the view really does change, for one frame. S292 found this site and wrote *"the caller is the answer"*, then stopped. <br>⛔ **WHY IT STALLED, AND WHAT UNBLOCKS IT.** It could only ever be observed with a human pressing the key: `bob_video.cpp` (S92) records that *a synthesised DIK tap carries no SDL modifier state*, so a Ctrl-modified key **cannot be reached from `BOB_KEYSEQ` at all**. S92 hit the same wall for `ALT+D`/`SHIFT+D` and solved it with an env that sets the state directly. Same fix here: **`MA_REVPADLOCK_AT=<n>`** fires `List6Toggle()` after n view initialisations, so the defect reproduces headlessly. Paired with a **one-shot `backtrace()`** at the reset site (`MA_TRACE_REVPAD=1`), the next run NAMES the caller instead of leaving it to inference. <br>⛔ **CORRECTION (2026-08-29): THE ROOT CAUSE ABOVE IS NOT CONFIRMED, AND I OVERSTATED IT.** `InitFlyingView` was never observed to run **at all** — in four reproduction attempts it produced zero calls, and that is not an instrumentation failure: the same runs printed **81 `[page]` lines**, so `EXTRAENV` reaches the child, and the counter fires on EVERY call when `MA_TRACE_REVPAD` is set. If the function never runs, it cannot be the thing stealing the view, so `Viewsel.cpp:2051` remains a PLAUSIBLE mechanism that has not been shown to be THE mechanism. <br>**Why the attempts failed, in order:** (1) quick-mission recipe reconstructed by hand — never reached 3D; (2) same, with `MA_QUICKMISS=3` — never reached 3D; (3) via `tacview_export.sh`'s proven recipe (flight PASSED, so 3D WAS reached) — still 0 calls, i.e. **`InitFlyingView` is not on the live-flight view path either**; (4) via `replay_corpus.sh`'s stage-4 reload — the `.cam` demonstrably loaded (`[replay] ACList=51 after the real LoadBlockHeader`, 43 replay lines) but the run **never entered the 3D playback VIEW** (only `[maximize] OnGoBig`), so the hook could not fire. <br>**SPRINT LIMIT (4) REACHED. What the next sprint needs, and it is not more theory:** a recipe that enters the 3D REPLAY VIEW, not one that merely loads the file. Until a run demonstrably reaches that view, every candidate here is unfalsified rather than supported. ⚠️ Do NOT re-derive the mechanism from reading `Viewsel.cpp` again — three sprints (S240, S292, this one) have now read that file and the missing piece each time was a REPRODUCTION, not an insight. | <br>⭐ **S332 (2026-08-29) — ANSWERED FROM THE LIVE KEY TABLE (PO-86's dump).** In the table the game itself loads: <br>• **`Left Ctrl+F6` IS NOT BOUND AT ALL** — no action, which is exactly why pressing it does nothing. <br>• **The bogie/enemy view is `F1` = `ENEMYVIEW`**, with `Left Ctrl+F1` = `RESETENEMYVIEW` and `Left Shift+F1` = `PREVENEMYVIEW`. <br>• Plain `F6` = `OUTSIDETOG`. `Ctrl+F1..F4` exist; **`Ctrl+F5` and `Ctrl+F6` do not.** <br>**So "CTRL+F6 does not show bogie view" is literally true and has a one-line cause: nothing is on that key.** There is ONE 3D key table (`FIL_3D_KEYBOARD_TABLE` via `Reg3dConv`); replay does not install a second one, and no code hardcodes `DIK_F6` — actions dispatch by table index. <br>⚠️ **WHAT THIS DOES NOT SETTLE, and I am not asserting it:** whether the ORIGINAL MiG Alley bound `Ctrl+F6` to something. The port loads the GAME'S OWN table file, so the bindings should already BE the original's — which argues the key was never bound — **but that is an inference, not a check.** The port's loader could be dropping entries (the dump does carry **39 bindings whose action index has no `KeyName`**, so the table holds more than the name list covers). <br>**DISCRIMINATOR:** compare the dumped table against the original under Wine (`port/ref/wine/`), or against the shipped manual. **If the original also has no `Ctrl+F6`, this item is a mistaken expectation and should be re-titled** — the answer to the PO would be *"the bogie view is F1"*. **If the original DOES bind it, the loader is dropping bindings**, which is a much more serious defect than one missing view key. <br>⚠️ Supersedes the earlier withdrawn root cause (`InitFlyingView` never observed to run across four attempts) — that investigation was chasing a code path when the question was answerable from the key table in one lookup. <br>⭐ **S334 (2026-08-29) — THE SERIOUS CASE. The port's key-table load DROPS ~40 bindings, and `Ctrl+F6` is one of them.** S332 said the discriminator was "if the original DOES bind it, the loader is dropping bindings". **It does, and it is.** <br>**Evidence chain, all from the game's own files:** <br>1. **`KEYBOARD/keys.xml`** (the original, human-readable): `<Command>OUTREVLOCKTOG</Command><Key>f6</Key><Modifier>Ctrl</Modifier>`. **`Ctrl+F6` is the outside-reverse-lock toggle — the bogie view the PO is asking for.** <br>2. **`KEYBOARD/keyb3d.bin`** (the binary the game actually loads, `FIL_3D_KEYBOARD_TABLE` = 0x7501): parsed with the `KeyMapping` layout (`KEYTEST.H:132`, `scancode:10, shiftstate:6, bitflag`), **entry 84 = scancode 0x40 (f6), shiftstate 4, bitflag 224**. 224 = `KeyName(112, OUTREVLOCKTOG)` × 2. **The binding IS in the data.** <br>3. **The LIVE table does not have it.** `MA_DUMP_BINDINGS` shows only 3 of the file's 4 F6 entries — `F6`/OUTSIDETOG, `Left Shift+F6` and `Right Shift+F6`/TOGGLEWOBBLEVIEW loaded; **shift 4 did not**. `OUTREVLOCKTOG` appears **zero** times in the whole live table, so the action is unreachable by any key. <br>4. **Not a blanket shift-4 failure:** `Left Ctrl+F1` = RESETENEMYVIEW loads correctly, so shiftstate 4 works in general. <br>⭐ **AND IT IS NOT ONE BINDING: the file has 616 `KeyMapping` entries and the live table has 576 — ~40 ARE BEING DROPPED.** `Ctrl+F6` is simply the one the PO happened to press. **Every one of those 40 is a key that silently does nothing.** <br>⚠️ **This also means S239 was wasted work.** `KEYMAPS.H:780` shows `KeyMap(OUTREVLOCKTOG, f6, CtrlL) //S239: GNOME eats Alt+F6` — a previous sprint moved this binding to Ctrl+F6 to dodge a GNOME shortcut. **The game loads its table from the FILE, not from that compiled list**, so the edit could never have taken effect, and the sprint that made it had no way to tell. <br>**NEXT (S334-S2): instrument `Reg3dConv` to report每 entry it skips and why** — 616 in, 576 out, and the 40-entry difference is the whole defect. Do NOT special-case `Ctrl+F6`: fixing one key while 39 others stay silently dead would look like a fix and be a symptom patch. <br>🔴 **S335 (2026-08-29) — S334 IS WRONG AND IS WITHDRAWN IN FULL. THE LOADER DROPS NOTHING, AND `Ctrl+F6` IS BOUND.** <br>**What the raw dump actually says:** `# unnamed action 224 = 0x40, 4` — **Ctrl+F6 IS in the live table**, mapped to action 224 = `KeyName(112,OUTREVLOCKTOG)`×2. <br>**How I got it wrong:** the dump writes an action with no name-table entry as a COMMENT (`# unnamed action N = ...`). My markdown generator skipped comment lines, so `ma_keyboard_commands.md` showed no Ctrl+F6 — **and I read the absence from my own generated document as absence from the game.** Then I compared the file's **616** entries against the **576 NAMED** bindings and called the difference "~40 dropped", **having forgotten the 39 unnamed ones I had myself excluded**. <br>**The real arithmetic:** 576 named + 39 unnamed = **615 live bindings from 616 file entries**. The single difference is legitimate: cell **(301,6) is written twice** in the file, so two entries share one slot. **Zero entries are out of bounds; `KEYSTUB.CPP:290`'s `breakif(bitflag==0)` never fires because the file contains no zero-bitflag entry.** **THE LOADER IS CORRECT.** <br>⚠️ **So "39 other keys silently do nothing" was false and is retracted.** They are bound and dispatched; they merely lack a NAME in the port's generated `ma_keyactions.inc`, which affects the dump and the rebinding file — not the game. <br>**WHAT REMAINS OF PO-78, restated honestly:** `Ctrl+F6` IS bound and IS dispatched to action 112 (`OUTREVLOCKTOG`), yet the PO reports no bogie view. **So the defect is in the ACTION'S BEHAVIOUR, not its binding.** Next: trace `OnKeyInput` with `MA_TRACE_KEY` while pressing Ctrl+F6 to confirm index 112 arrives, then follow what `OUTREVLOCKTOG` does — this is where `VM_OutRevPadlock` (the reverse-padlock view state) lives. <br>**Second, separate defect (real, small):** `ma_keyactions.inc` is missing at least `OUTREVLOCKTOG` (KEYMAPS.H:228 carries a `/**/` prefix that the generator may be mishandling). 39 actions are unnamed; a rebinding file cannot reference them by name.
| PO-84 | ✅ **FIXED (S327, cross-port from BoB S276): every `.acmi` MA has ever exported shows aircraft flying BACKWARDS in Tacview** | **PO 2026-08-29**, found on the BoB export (*"aircraft going backwards"*) and confirmed present in MA. <br>**Tacview orients the aircraft model from the `Yaw` field ALONE — `Heading` does not affect it — and reads that field in the OPPOSITE rotational sense from the compass convention both exporters compute.** <br>**PROVEN BY CONTROL, not by reading:** a synthetic `.acmi` with four aircraft all flying due east along +U, differing ONLY in the angle encoding: `yaw=090 hdg=090` → **backwards**; `yaw=270 hdg=270` → **nose-first**; `yaw=090 hdg=270` → **backwards**; `yaw=270 hdg=090` → **nose-first**. The two correct cases share `yaw=270` and differ in Heading, which isolates the field and the sign in one pass. Control files kept: `control_straight.acmi`, `control_axes.acmi`, `control_yaw.acmi`. <br>**CONFIRMED FOR MA RATHER THAN ASSUMED** (the engines have forked before — BoB's velocity constant is 10000, MA's 25): on MA's own exports `|yaw - course_from_UV|` medians **5.47°** (`260828.acmi`) and **0.70°** (`ma-1v1-long.acmi`), against **52.9°** and **43.5°** for the negated form. So MA writes the same compass sense BoB did, and `ma_acmi.cpp:195` was character-for-character BoB's pre-fix line. <br>**WHY IT HID:** yaw 000 is a fixed point of the negation, so north-south legs always looked correct and only the east-west component ever reversed. That is why the PO first reported it as "30 degrees off" (a track near north) and only later as "backwards" (a track near east) — one bug, not three. <br>⚠️ **WHY MY ANALYSIS MISSED IT TWICE:** I checked the file for INTERNAL consistency — yaw against the course implied by the U/V in the same file — and it passed at 3.8°, so I twice told the PO the export was fine and the fault was probably their Tacview view settings. **That test is structurally incapable of catching this: yaw and course share the same convention, so they agree with each other while both disagree with Tacview.** A self-consistency check cannot validate a convention. The PO's dead-straight control leg had an unambiguous right answer; a curving dogfight never does. <br>**FIX:** write `360 - yaw` into the Yaw field, leaving Heading as the true compass value. `MA_ACMI_RAWYAW=1` reverts. Existing files repaired in place-by-copy: `260828_FIXED.acmi` (18,820 samples), `bob_260829_kill_FIXED.acmi` (6,932). |
| PO-82 | 🔴 **Objects and effects lose their texture and draw WHITE** (recurrence of **S131**) | **PO 2026-08-29**, video `~/Videos/260829_ma_white.mp4` (156 s, 1920×1080@60) — *"when I start striking the bogie the impact flashes are white blocks, then the smoke and explosion are white, starting at about 1:40"*. <br>**FRAME EVIDENCE (extracted, not inferred):** `t=95s` everything correct. `t=101s` the tracer/impact stream is a diagonal chain of **flat white squares, some YELLOW**. `t=106s` the smoke trail is an **opaque white ribbon**. `t=108s` the explosion is a **flat opaque white QUAD** with textured yellow flame blobs inside it — *while a correctly textured smoke puff renders in the SAME frame*. Cockpit, gauges, canopy and terrain stay correctly textured throughout. <br>⚠️ **CORRECTION to my first filing:** I wrote that "the cockpit, canopy frame and airframe render pure white". That was wrong — I described the screenshots without opening them. `Screenshot 10-54-29.png` actually shows a **parachute canopy** drawing untextured white over correctly textured terrain. The defect is **specific objects/effects losing their texture, never a global GL failure.** <br>⭐ **THIS IS S131, WHICH IS ALREADY IN THE CODE.** `bob_video.cpp:2187` records the earlier PO report *"after a few passes all the objects turned white"* and its conclusion: **"a white object is an UNTEXTURED one — it draws in its vertex colour."** That matches the yellow squares exactly (yellow vertex colour, no texture). S131 could not reproduce it and left a detector behind. <br>**MECHANISM — CONFIRMED BY READING, two lines:** <br>1. `compat/ma_d3d_exec.cpp:73` — `if (!s || !s->sbits || s->sw <= 0 || s->sh <= 0) return 0;` with the comment *"Returns 0 for an unknown handle or a surface with no pixels yet — both mean 'draw this untextured' rather than 'fail'."* <br>2. `compat/bob_video.cpp:2236` — `else if (st->tex && !noTex) ma_gl_bind_exec_texture(st->tex); else glDisable(GL_TEXTURE_2D);` <br>So a texture handle that fails to resolve **silently** becomes an untextured draw in vertex colour = white. The engine names textures by `D3DRENDERSTATE_TEXTUREHANDLE` in execute buffers (`Win3d.cpp`: 35 uses, 36 execute-buffer uses, **zero** `DrawPrimitive`), decoded at `ma_d3d_exec.cpp:250`. <br>**PRIME SUSPECT — the engine EVICTS textures and the PO changed graphics prefs:** `direct_3d::ReleaseTextures()` (`Win3d.cpp:5433`) RELEASEs every vidram texture surface and zeroes `NumVidTextures`, and it is called from **`SetTextureQuality()` (`Win3d.cpp:6729`)** plus 3 other sites. If a released surface leaves its handle registered with `sbits == NULL`, every later draw with that handle goes white. That is a direct causal path from "I set the primary-graphics preference" to "objects turn white", and it is testable. <br>⚠️ **THE EXISTING DETECTOR FIRED — AND IT PROVES ALMOST NOTHING.** I first wrote here that it could not fire; that was wrong. In the live session it printed `[tex] TEXTURES LOST: only 2378473 of 23784731 batches are textured`. **Read the arithmetic before believing it:** 2378473 × 10 = 23,784,73**0** vs a total of 23,784,73**1** — it tripped by a margin of ONE batch, on a counter that is **CUMULATIVE SINCE LAUNCH** (`static long tex, untex` at `bob_video.cpp:2190`). With 23.8 M batches of history, an event beginning at 1:40 of a session cannot shift that ratio appreciably, so the fire reflects the long-run average drifting across an arbitrary line, NOT the moment textures were lost. Worse, the 10% premise is unfounded: the engine deliberately emits many untextured batches (`Win3d.cpp` sets `STATE_DATA(TEXTUREHANDLE, NULL, ...)` at ~17 sites), so "under 10% textured" was never a definition of unhealthy. It also latches (`reported = 1`), so it speaks once per process and never again. **This instrument cannot attribute its own alarm to the defect — treat its output as neither confirmation nor refutation, and replace it with a WINDOWED rate (per-N-batch or per-frame), not a cumulative ratio.** <br>**NEXT STEP (instrument, do not guess):** at `ma_tex_desc` count the three rejection reasons SEPARATELY — unknown handle (`s == 0`), registered-but-`sbits == NULL`, bad dimensions — and report per-handle. The three have different fixes; the current code collapses them into one silent `return 0`. | <br>**S328-S2 (2026-08-29) — INSTRUMENT RUN, and the baseline is CLEAN: `resolved 40000+, FAILED 0` (unregistered 0, released/no-pixels 0, bad-dims 0)** on a 45 s flight via `EXTRAENV="MA_TRACE_TEXFAIL=1" port/tacview_export.sh` (rode the known-good recipe rather than rebuilding one; export gate PASSed, 178 frames, `.acmi` structurally valid). <br>⚠️ **DO NOT READ THAT ZERO AS "THE BUG IS GONE".** The success counter is what makes it readable at all — 40,000 resolutions prove the instrument RAN, so this is a real zero and not silence. But **the recipe never exercises the suspected trigger**: an automated flight never opens the graphics preferences, so it never calls `SetTextureQuality()` → `ReleaseTextures()`, which is the prime suspect for releasing a surface out from under a live handle. The PO's repro STARTS with that (setting primary graphics + filtering to all). **This is why S131 could not reproduce it either** — same blind recipe, and S131 concluded "could not reproduce" where the honest statement was "did not exercise the trigger". <br>**NEXT (S328-S3):** a run that CHANGES the texture-quality preference mid-session and then flies. Until then this instrument will report healthy forever, and that is a property of the recipe, not of the code. <br>**S328b:** the periodic report fired only every 20,000 calls, so the final partial block went unreported — the run above showed "40000" when the true total was 40,000–60,000, and a LATE collapse lives exactly in that unreported tail. Now also reports at exit (`FINAL`). <br>**S328-S3 (2026-08-29) — a REAL LATENT DEFECT found and fixed, but ⚠️ NOT SHOWN TO BE THE PO'S CAUSE. I first wrote "ROOT CAUSE FOUND" here; the verification run refuted that and the claim is withdrawn.** The texture-handle registry was WRITE-ONLY, so a destroyed surface left a DANGLING POINTER that `ma_tex_desc` would dereference. `ma_d3d_texture_register` fills `s_texSurf[handle]` and **nothing ever cleared it**; `sbits` is freed only in `~IDirectDrawSurface` (`ddraw_legacy.h:139`), which destroys the whole object. So after any surface destruction the registry still named it, and `ma_tex_desc` read `s->sbits / s->sw / s->sh` **out of freed memory**. <br>**This is also why the S328 instrument reported `FAILED 0` even with a FORCED `DoReleaseTextures()`** (`MA_FORCE_TEXRELEASE`, added this sprint, fires the PO's exact graphics-preference path mid-flight): the `!s->sbits` guard reads freed memory, which usually still holds the old non-null pointer, so **the check PASSES** and hands the GL uploader a descriptor pointing at freed pixels. **The three causes it counted never included the real one.** A clean run through the trigger was not evidence of health — it was the bug hiding inside the instrument's blind spot. <br>⚠️ **THE VERIFICATION RUN REFUTED THE CAUSAL STORY.** With the fix in, the same forced `DoReleaseTextures()` produced **ZERO unregister events** — so that call destroys **no** `IDirectDrawSurface` objects at all. The engine's `ReleaseTextures()` releases D3D texture interfaces while the compat surfaces live on. **So the graphics-preference path does NOT create dangling handles, and my claim that it did is withdrawn.** No surface was destroyed at any point in a 45 s flight, which means the dangling-pointer defect, though real, has not been shown to fire in the PO's scenario at all. <br>**PO-82's cause is therefore still UNIDENTIFIED.** What is established: (a) a white object is an untextured draw in its vertex colour (S131, confirmed by the PO's frames — some impact sprites are YELLOW); (b) an unresolved handle silently becomes `glDisable(GL_TEXTURE_2D)`; (c) the registry could hand out freed memory if a surface ever were destroyed; (d) nothing observed so far destroys one. <br>⚠️ **AND THE ZERO IS ITSELF UNPROVEN:** the unregister trace only prints when a dying surface MATCHES a registry slot. A destructor that ran for an unregistered surface prints nothing, so "0 events" cannot yet distinguish "no destructions" from "destructions of unregistered surfaces". **Next: count destructor calls unconditionally** — the same prove-the-instrument-can-speak discipline that this item has now needed three times. <br>**FIX:** `ma_d3d_texture_forget(surface)` clears every slot naming a dying surface, called from `~IDirectDrawSurface` **before** the pixels are freed. O(4096) once per destruction, off the hot path. After it, an unknown handle resolves to 0 and draws untextured — still wrong, but *deterministically* wrong and now COUNTED as `unregistered`, instead of silently reading freed memory. <br>⚠️ **NOT YET CONFIRMED AGAINST THE PO'S REPRO.** The fix is correct by construction and the mechanism is proven by reading, but nothing has yet reproduced the white textures on demand: the forced-release run stayed clean *because* the guard could not detect the dangling case. The honest next step is a run that destroys surfaces and then keeps drawing with the old handles, verifying the `unregistered` counter now RISES where it previously stayed silent.
| PO-81 | 🔴 **Spinning instrument-panel compass — HYPOTHESIS: `SetTimedAnim` has NO WRAP HANDLING, and the gyro card is a full-range wrapping field** (not yet run) | **PO 2026-08-29**: *"when I press keyboard ENT to see the F86 instrument panel, the compass is always spinning like a pinwheel"*, and crucially *"during takeoff in campaign 1 the ma compass works — it works while the jet is on the ground"*. <br>**TWO DRIVERS WRITE THE SAME FIELD, both unconditional in the shipped Rowan source:** `MODEL.CPP:508` `AddAutoDial(&acgyrocompass, &Inst.HdgAngle, 0..360, scaler -182.04)` sets the TARGET from heading, and `MODEL.CPP:550` `AddTimedDial(&acgyrocompass, ARC_1SEC, ARC_1SEC<<1)` LAGS toward it. They are meant to cooperate — the timed dial reads the destination field as its `Desired` (`MODINT.CPP:1804/1820`) and walks toward it. (Neighbouring `AddTimedDial`s at :548 and :551 were deliberately commented out by Rowan, so this one is intentional.) <br>⭐ **THE DEFECT: `TimedDialSpec::SetTimedAnim` computes `gap = Desired - SrcVal` (`MODINT.CPP:1827`) as a PLAIN LINEAR DIFFERENCE, with no modular/shortest-arc handling anywhere in the function.** `acgyrocompass` is a 16-bit field whose scaler (−182.04) maps 360° onto the FULL 65,534-count span, so it **wraps**. At a wrap crossing `gap` becomes ≈±65535 instead of ≈0, and the dial takes the LONG WAY ROUND — one complete rotation of the card per crossing. <br>⭐ **THIS EXPLAINS THE PO'S OWN CLUE, which is why it is the leading hypothesis rather than a guess.** On the ground heading is essentially constant → no wrap crossing → the card settles, which is exactly what the PO reports. Airborne the heading changes continuously → repeated crossings → the card never settles and spins. **A theory that did not explain the ground/air split would be wrong on the PO's evidence alone.** <br>⚠️ **NOT YET CONFIRMED — no run.** The display was held by julia's Spa attribution run for this whole sprint. `MA_TRACE_COMPASS` already exists (`MODINT.CPP:1061`) and prints `hdg`, `accompass` and `acgyrocompass` together. <br>**PREDICTION, stated before the run:** across a heading wrap, `HdgAngle` will move a few degrees while `acgyrocompass` sweeps most of the 16-bit range in one direction; `accompass` (the UByte magnetic card, AutoDial only, no timed lag) will NOT do this. **If both cards misbehave, the timed dial is not the cause and this is withdrawn.** <br>⚠️ **Noted, deliberately NOT fixed:** `SetTimedAnim` READS the field signed (`SWordP`, :1820) and WRITES it unsigned (`UWordP`, :1873). At 16 bits the stored bits are identical, so it is very likely benign — but it is an asymmetry, and changing it speculatively while chasing a different defect is how one verified fix becomes two unverified ones. | <br>**S331-S2 (2026-08-29) — FIX PREPARED, DEFAULT OFF, NOT YET VERIFIED.** `MA_COMPASS_WRAPFIX=1` folds `gap` into `[-32768, +32767]` in `TimedDialSpec::SetTimedAnim`, so a wrapping dial always takes the SHORT way round instead of walking the long arc at every crossing. <br>**Deliberately narrow:** applied only when `dist == 65535` (16-bit fields) AND only under the env flag. Most timed dials on that list are **not angular** — `acoilpress`, `acfuelpress`, `achydraulicpress`, `accabinpress`, `acoxygenpress` — and folding their gap would be a silent behaviour change to instruments nobody has reported a problem with. **A fix that quietly alters five working dials to repair one broken one is not a fix.** <br>⚠️ **STILL NOT VERIFIED — the display was held by julia's E80 runs for this entire sprint, so no A/B has been run.** The test is unchanged and already stated: with `MA_TRACE_COMPASS`, across a heading wrap, `acgyrocompass` should sweep most of the 16-bit range with the flag OFF and track `HdgAngle` smoothly with it ON, while `accompass` (no timed lag) is unaffected either way. **If `accompass` also misbehaves, the timed dial is not the cause and both the hypothesis and this fix are withdrawn.** <br>⚠️ **S331-S3 A/B RAN AND IS INCONCLUSIVE — the recipe never exercises the trigger.** Both arms (`MA_COMPASS_WRAPFIX` off and on) via `EXTRAENV` on `tacview_export.sh`, 60 s: **8 compass samples each, and `hdg` max step = 0.0°.** The aircraft flies STRAIGHT, so the heading never changes, let alone crosses the 0/360 wrap the hypothesis is about. **Nothing was tested.** <br>**What the run DOES show:** with heading constant, `acgyrocompass` tracks `hdg × -182.04` to within **3 counts median, 6 max** (≈0.03°) in BOTH arms. So the AutoDial + TimedDial pair is accurate when nothing wraps — consistent with the PO's *"it works while the jet is on the ground"* — and the wrap fix changes nothing when there is no wrap, which is the correct null result for a default-off flag. <br>⚠️ **This is the PO-82 failure repeating: an automated flight that cannot reach the condition, and a clean result that means "not exercised" rather than "not broken".** Recorded as such rather than logged as a pass. <br>**NEXT (S331-S4): a flight that TURNS.** The requirement is explicit — the heading must sweep through 0/360 at least once. `tacview_export` at any duration will not do it; the recipe needs a route or combat leg (e.g. `MA_CAMP_FLY`, or the attack-pattern drive) where the aircraft manoeuvres. **Until a run shows `hdg` actually crossing the wrap, neither the hypothesis nor the fix has been tested at all.** <br>**S331-S4 — GATE WRITTEN: `port/compass_wrap.sh`.** S3 failed because the recipe flies straight; this one **flies a campaign mission** (`MA_CAMP_FLY=1` drives the frag→Fly chain and the aircraft follows a waypoint route, so it turns), using the font-proof campaign click sequence `30,r3;65,#1055;100,#2063:1` lifted from `asan_all.sh` rather than hand-built — the recurring lesson that four hand-made recipes have failed where copied ones work. <br>⭐ **IT REFUSES TO REPORT A VERDICT UNLESS THE HEADING ACTUALLY CROSSED THE WRAP.** The gate detects a crossing as a single-step heading change >180°, and with none it exits **2 INCONCLUSIVE** saying *"the condition under test did not occur, so neither arm means anything — this is the S331-S3 failure; do not read a clean result as a pass."* **That check is the entire point:** without it this gate would have printed a confident PASS on the S3 data, which tested nothing. <br>**Verdict logic:** PASS only if the OFF arm's median |gyro − heading×−182.04| exceeds 2000 counts (the card genuinely diverges) AND the ON arm cuts it to under a quarter. If the OFF arm does NOT diverge it exits 1 with **FAIL/RETHINK: the wrap is not what makes the card spin** — the hypothesis being wrong is a first-class outcome, not an error. <br>⚠️ **`MA_CAMP_FLY` ADVANCES THE CAMPAIGN**, so the gate stashes `SaveGame/Auto Save.sav` and restores it on EXIT (trap), exactly as `asan_all.sh` does per S81. The PO's campaign save is irreplaceable and a diagnostic must never eat game state. <br>**Not yet run — the display was held by julia's Spa attribution for this sprint.** PO-81 has now used its 4 sprints and rotates; the gate is ready for the next GL slot.
| PO-86 | ✅ **DONE: keyboard-command reference → `~/Documents/260829/ma_keyboard_commands.md`** | **PO 2026-08-29.** A written list of every keyboard command the game responds to, as a Markdown file in `~/Documents/260829` (the directory does not exist yet — create it). <br>**THE RAW MATERIAL ALREADY EXISTS — do not hand-compile a list from memory or from the manual.** <br>1. **`SRC/H/KEYMAPS.H`** holds the authoritative action list as `KeyName(index, NAME)` macros; the `KeyVal3D` constant is `index*2` (`ma_keybind.cpp:6`). That is the set of ACTIONS. <br>2. **`MA_DUMP_BINDINGS=1`** already writes the live bindings as `ACTION = scancode[, shift]` (`ma_keybind.cpp:15-21`), read back from `KeyMap3d::mappings[scancode][shiftstate]` AFTER the game's own table load. That is the ACTION → KEY half, taken from the running game rather than guessed. <br>3. The remaining work is turning DirectInput scancodes into key NAMES (`0x50` → `Down Arrow`), for which `SRC/H/DINPUT.H` carries the `DIK_*` constants. <br>⚠️ **The keys the PO actually uses must appear and be verified**, since they are the ones with known behaviour: **ENT** (instrument panel — see PO-81), **ALT+X** (exit the 3D). If a key the PO has demonstrably used is absent from the generated list, the generator is wrong, not the PO — that is the acceptance check. <br>⚠️ **Say which keys are the PORT's own, not Rowan's.** `Ctrl+ESC` is a port-added hard exit hatch (`bob_video.cpp`, "a hard exit hatch while the UI loop isn't wired"), and there may be others. A reference that silently mixes original and port-added bindings misleads on both. <br>**Done when** the `.md` lists every action with its key(s), marks unbound actions as unbound rather than omitting them, distinguishes 3D/flight bindings from front-end/UI ones, and flags port-added keys. | <br>⭐ **DELIVERED: `~/Documents/260829/ma_keyboard_commands.md`** (620 lines). **Every number machine-read; nothing typed from memory or the manual.** <br>**Method:** actions from `KEYMAPS.H` (`KeyName(index,NAME)` — 209 live, 33 `//DEADCODE` excluded); bindings from **`MA_DUMP_BINDINGS=1` during an actual flight**, read back out of the live `KeyMap3d::mappings[scancode][shiftstate]` array AFTER the game loaded its own table — so these are the bindings **in force**, not a config file's intentions; key names from the 120 `DIK_*` constants in `DINPUT.H`. Dumped to `MA_CONTROLS=/tmp/...` so the PO's own `controls.cfg` was never touched. <br>**Modifier encoding resolved rather than assumed:** the shift state is an **INDEX, not a bitmask**, despite `KEYTEST.H:107` calling it a "Shift bit pattern" — `currshifts = index>>1` (`STUB3D.CPP:1382`), so 1=ExtShift, 2=Left Alt, 3=AltGr, 4=Left Ctrl, 5=Right Ctrl, 6=Left Shift, 7=Right Shift, from `KeyName(1..7)`. Reading it as a bitmask would have mislabelled every modified binding. <br>⭐ **ACCEPTANCE CHECK PASSED on the PO's own keys, and it corrected one:** `Numpad Enter` → **INSTVIEW2** is the PO's *"press keyboard ENT to see the F86 instrument panel"*, while plain `Enter` → **PADLOCKTOG**. **"ENT" is the NUMPAD Enter, and it is a different key from Enter** — worth carrying into PO-81, whose repro starts with that keypress. `Left Alt+X` → **EXITKEY** confirms the ALT+X path. <br>**576 bindings, 175 distinct actions.** <br>⚠️ **Stated limits, in the document itself:** (a) these are the **3D/flight** bindings from the table `Reg3dConv` loads on entering the cockpit — **front-end/menu keys are handled elsewhere and are NOT covered**; (b) **39 live bindings map to an action index with no `KeyName` entry** and are listed as `# unnamed action N` rather than dropped; (c) 1 live action has no key bound, listed rather than omitted; (d) `Ctrl+ESC` is a **port-added** compat-layer exit hatch and correctly does NOT appear, since it is not in the game's key table. |
| PO-85 | ✅ **DONE (S330): an unhandled control type in `ma_ole_draw_all` now ANNOUNCES ITSELF — the PO-83 class of bug made self-reporting** | **Generated by PO-83's fix.** That bug was a control type with **no `else if` in the dispatch chain**: hosted, classified, populated, never drawn, and nothing said so. Four sprints to find a missing branch. <br>**RESOLVED THE DEFERRED QUESTION WITHOUT GUESSING.** PO-83 flagged that `CT_SCROLL` and `CT_SPIN` are also absent from `ma_ole_draw_all` and deliberately did not add branches. Reading found the answer for one of them: **`CT_SCROLL`'s absence is INTENTIONAL and evidence-backed** — S140 (`ma_olecontrol.cpp:1010`) records that drawing listbox scrollbars on the front-end path put spurious bars across the title screen and **parity caught it** (title, 2839px, bbox 530,210-635,310). So no change there, and the reason is now linked from this entry rather than rediscovered. **`CT_SPIN` carries no such rationale** — it is created, hosted, prop/invoke-routed and drawn on the toolbar path, but simply missing here. <br>**BUILT:** `MA_TRACE_NODRAW=1` reports any hosted control whose type has no branch, once per type, with its id and rect — excluding `CT_SCROLL`, whose omission is deliberate and would be noise. <br>**MEASURED: 600+ dispatches across prefs, variants and campaign-map screens, `0 with no branch`.** No `CT_SPIN` reaches this path on those screens, so nothing is silently lost there today — but if one ever does, it will say so instead of vanishing. <br>⭐ **THE ZERO IS ANCHORED, AFTER TWO FAILURES TO ANCHOR IT.** A detector that has never fired proves nothing, so it also counts controls that REACH the chain and prints both. Getting that right took three attempts, each the same mistake: (1) `atexit` — printed NOTHING, because `MA_SHOT`-style captures leave via `_exit()`, which skips atexit handlers; (2) periodic every 2000 — still nothing, the run never reaches 2000 dispatches; (3) first-dispatch **plus** every 200 — speaks. **The instrument meant to make a zero trustworthy was itself silent twice.** Same correction PO-82's instrument needed (S328b), in a new place, on the same day. |
| PO-83 | ✅ **FIXED (S329-S2): `ma_ole_draw_all` had NO `CT_RADIO` BRANCH — every radio group outside a toolbar dialog was invisible** | **PO 2026-08-29**, screenshot: only the labels **"F86 1"** / **"F86 2"** and **Back**; no selectable variants. <br>⭐ **IT IS NOT A LIST — it is two RADIO GROUPS.** `SRC/H/VARIANTS.H`: `CVariants` (IDD_VARIANTS) hosts `IDC_RRADIO_F86VARIANTS0`, `IDC_RRADIO_F86VARIANTS1` (+ a commented-out Mustang group, dead since rdh 10/1/99) and three `CRStatic`s. `VARIANTS.CPP:125-141` fills EACH group with three buttons — `IDS_F86a`/`IDS_F86e`/`IDS_F86f` (F-86A/E/F) — then `SetCurrentSelection(MMC.squad[N].actype)`. **The two labels the PO sees are the group statics; what is missing is the three radio buttons under each.** <br>**`AddButton` is NOT stubbed** — `RRADIO/RRADIOC.CPP:499` is a real implementation (copies the text, `m_list.AddTail`, `InvalidateControl`), and the SAME call path is used by the Locker (`LOCKER.CPP:228+`) and Ground View (`GRNDVIEW.CPP:272+`) screens, which DO work. So the drawing/one-control mechanism is sound and must not be blamed without evidence. <br>**Discriminating test, in this order:** <br>1. **Is the OLE control HOSTED for IDD_VARIANTS at all?** `GETDLGITEM` is `(&m_##name)` (`RDIALOG.H:18`) — it returns the address of a member, so it is NEVER null and CANNOT fail loudly. The wrapper `CRRadio::AddButton` (`H/RRADIO.H:55`) dispatches through OLE and **silently no-ops if the underlying control was never created** — which produces exactly "statics render, buttons absent". Use this session's `MA_TRACE_OLE` / per-control hosting dump on this dialog. <br>2. Only if hosted: does `RRADIOC.CPP:499` actually get reached, and is `text` non-empty (i.e. did `LoadString(IDS_F86a/e/f)` resolve)? An empty string still adds a row, so "reached with empty text" and "never reached" are different bugs. <br>⚠️ Same family as **PO-77** (listbox backing) and the BoB R3.8 hosting question — but do NOT reuse R3.8's conclusion (row painting); that was a listbox and this is not. | <br>⭐ **S329-S1 (2026-08-29) — MEASURED: the variant groups ARE fully populated. This is NOT a data or population failure.** Reproduced headlessly (no GL, no PO) with `BOB_CLICKSEQ="40,r1;60,r1;90,r0"` under `SDL_VIDEODRIVER=dummy` — that click path reaches the mission-setup screen carrying the `Back / Variants / Fly` menu, and constructing the dialog fires: <br>`[radio] AddButton ctrl=0xa22dcc0 "F86A" / "F86E" / "F86F"  -> now holds 1,2,3 item(s)` <br>`[radio] AddButton ctrl=0xa233720 "F86A" / "F86E" / "F86F"  -> now holds 1,2,3 item(s)` <br>**Both groups reach the CONTROL (not merely the wrapper), with three correctly-resolved strings each** — so `IDS_F86a/e/f` resolve, `GETDLGITEM` returns a usable control, the OLE dispatch is NOT no-opping, and `CRRadioCtrl::AddButton` runs. Every candidate in my original filing for "the buttons were never added" is eliminated. <br>**So the defect IS the BoB R3.8 shape after all** — created, hosted, populated, and not visible. My first filing warned *"do NOT reuse R3.8's conclusion"*, which was right as a discipline (it was not yet measured) and is now settled the other way BY measurement rather than by analogy. <br>**Instrument (S329):** `MA_TRACE_RADIO` now traces BOTH sides — a pre-existing wrapper trace (`AddButton ctrl=…`) and a new control-level one (`now holds N`). The pair is what separates "the wrapper was called" from "the control received it", which `GETDLGITEM` being `(&m_##name)` — never null, cannot fail loudly — otherwise makes indistinguishable. Proven to speak first on a KNOWN-GOOD screen (quick-mission's Scenario/UN/Red group, 3 items) before being trusted here. <br>⚠️ **THE PAINT HALF IS UNRESOLVED, and its silence proves nothing.** `CRRadioCtrl::OnDraw` emitted **no** lines for the variant groups — but it emitted none for the known-good quick-mission group either, so `OnDraw` simply does not run in this headless mode. **Do not read that as "never painted".** <br>**NEXT (S329-S2):** re-run the same click path with real GL (`gl-lock`, no dummy driver) so `OnDraw` can fire, and read the reported `items=`, `bounds=` and `hWnd=`. Three outcomes with three different fixes: no `OnDraw` at all → the control is not drawn; `items=0` at paint time → the list is cleared between init and paint; `items=3` with a degenerate or off-screen `bounds` → the R3.8 answer exactly. | <br>⭐ **S329-S2 (2026-08-29) — ROOT CAUSE AND FIX, verified in pixels.** `ma_ole_draw_all` (`ma_olecontrol.cpp:820`), the MAIN OLE control drawer, dispatched `CT_STATIC / CT_EDIT / CT_EDTBT / CT_TABS / CT_BUTTON / CT_COMBO / CT_LISTBOX` — **and no `CT_RADIO`**. Only `ma_ole_draw_toolbar` had that branch, which is why radios appeared on toolbar dialogs and nowhere else. The variants dialog's groups were created, hosted, classified and populated, then silently never drawn. <br>**The measurement chain that got there, each step eliminating a candidate:** (1) `AddButton` reaches the CONTROL with three correct strings per group → not a population or string-resource failure; (2) `CRRadioCtrl::OnDraw` fires ZERO times even under real GL → not a paint-content failure; (3) 4 controls ARE `HOSTED as CT_RADIO` → not CLSID recognition; (4) the dispatcher issues **ZERO** radio draws → the fault is the dispatch itself. Reading the branch list then found the missing case immediately. <br>**FIX:** add the `CT_RADIO` branch to `ma_ole_draw_all`, calling `ma_radio_draw` exactly as the toolbar drawer does. <br>**VERIFIED IN PIXELS, not just in logs:** after the fix `OnDraw` fires with `items=3 bounds=(0,0)-(462,26) hWnd=1`, and the captured frame shows **F86 1 → F86A / F86E (selected) / F86F** and **F86 2 → the same**, where the PO saw only the two captions and Back. <br>⚠️ **SCOPE — this was never variants-specific.** Any radio group on a non-toolbar dialog was invisible for the same reason, so other screens using `AddButton` (`LOCKER.CPP:228+`, `GRNDVIEW.CPP:272+`) were affected too and should now render. <br>⚠️ **`CT_SCROLL` and `CT_SPIN` are ALSO absent from `ma_ole_draw_all`** while present in the toolbar drawer. **Not touched** — that is a separate claim needing its own evidence, and adding branches speculatively is how a one-line fix becomes three unverified ones. Worth a sprint. <br>⚠️ **Cosmetic follow-up:** each group renders on an opaque BLACK backing box rather than over the dialog art. The buttons are legible and selectable, so this is not the PO's reported defect, but it is not right either. |
| PO-81 | **Instrument panel: the compass spins like a pinwheel** | **PO 2026-08-29.** Pressing **ENT** to show the F-86 instrument panel: *"the compass is always spinning like a pinwheel"*. <br>⭐ **PRIME SUSPECT — `acgyrocompass` HAS TWO DRIVERS ON THE SAME ANIMATION FIELD:** <br>`MODEL.CPP:508` `AddAutoDial(adptr,&adptr->acgyrocompass,&Inst,&Inst.HdgAngle,0.0,360.0,-182.04)` <br>`MODEL.CPP:550` `AddTimedDial(adptr,&adptr->acgyrocompass,ARC_1SEC,ARC_1SEC<<1)` <br>An **auto** dial slaved to heading AND a **timed** dial advancing on a 1-second arc, both writing `acgyrocompass`. A timed dial on a compass card is continuous rotation — which is what a pinwheel is. ⭐ **And the timed version looks like it was MEANT to go:** `MODINT.CPP:1155` carries exactly this animation dead-coded — `//DeadCode RJS 29Jun98 animstep = SetTimedAnim(GyroCompass,TimerGyro,adptr->acgyrocompass,ARC_1SEC,ARC_1SEC<<1);` — so the driver was retired in one place and its registration survived in the other. <br>**Already excluded, so nobody re-treads it:** `shape::docompass` (`3DCOM.CPP:7671`) is a **STUB** (`instr_ptr += sizeof(DOCOMPASS)` and nothing else), so the 3D opcode path is not the source of the motion; and it is **not** the BAMS-vs-degrees units trap — `Inst.HdgAngle` is `FP` **in degrees** (`MODINST.H:96`) from `Rads2Degs(pModel->fHdg)` (`MODINT.CPP:825`), matching the AutoDial's 0..360 range. <br>⚠️ **There are TWO compass fields:** `accompass` (magnetic, auto-dial only) and `acgyrocompass` (auto **+** timed). **Establish which one the PO sees spinning first** — only the gyro has the duplicate driver, so if the magnetic card is the one spinning, this suspect is wrong. <br>⛔ **THE PRIME SUSPECT IS REFUTED — BOTH ANIM VALUES ARE CORRECT (2026-08-29).** `MA_TRACE_COMPASS` in `SetCockpitAnims`, where the fields are written, over a real flight: <br>`[compass] hdg=23.5 deg accompass=16 acgyrocompass=-4277` <br>`[compass] hdg=13.5 deg accompass=9  acgyrocompass=-2455` <br>Both track heading **exactly**: `23.5/360*255 = 16.6` → 16 ✓ and `23.5 * -182.04 = -4278` → -4277 ✓. **No timed accumulation is visible** — the duplicate `AddTimedDial` is not running the value away, so it is not the pinwheel. <br>⭐ **So the spin is DOWNSTREAM of these values, in how the panel RENDERS them** — not in the dial arithmetic. That is where the next sprint goes. ✅ **AND THE ENT VIEW IS THE 3D COCKPIT, so the correct values DO reach what the PO sees.** `ENT` (numpad) is `INSTVIEW2` → `HandleQuickView(QV_Instruments)` (`Viewsel.cpp:307`), which is not a separate 2D panel at all: <br>`viewnum.viewmode = VM_Inside; quickviewrec = &VRNorth; pitchfiddle = -ANGLES_30Deg;` <br>— the ordinary inside/cockpit view, pitched 30° down to look at the panel. So the compass the PO watches is the 3D cockpit instrument, fed by the `accompass`/`acgyrocompass` values measured correct above. <br>⭐ **PO CLUE (2026-08-29): "during takeoff in campaign 1 the ma compass works — it works while the jet is on the ground."** So it is correct on the ground and wrong in the air, which is a much sharper discriminator than the symptom alone. <br>⚠️ **But note my in-flight trace above ALREADY showed correct values while AIRBORNE** — that run was a QUICK MISSION, not campaign 1. So the split may be campaign-vs-quick-mission rather than ground-vs-air, and the two must be told apart before either is chased. <br>⛔ **The obvious range theory is RULED OUT.** A tempting explanation is that `HdgAngle` goes negative in flight and the dial clamps it (`SetDialValue` clamps, it does not wrap: `if (theValue2 < minval) theValue2 = minval`). It cannot: `fHdg = CalcAngle(...)` and `CalcAngle` returns **0..2π** on every one of its eight quadrant branches (`a`, `F1PIE2±a`, `FPIE±a`, `F3PIE2±a`, `F2PIE-a`), so `HdgAngle` is **0..360** — exactly the AutoDial's configured range. Nothing to clamp. <br>✅ **CAMPAIGN 1 TRACED (2026-08-29) — BOTH FIELDS ARE STILL CORRECT, INCLUDING IN THE AIR.** 31 samples through a campaign takeoff, 30 of them at a steady heading of ~301.45°: <br>`accompass` **213 → 213, rock stable** — and `301.45/360*255 = 213.5` ✓ <br>`acgyrocompass` 10652 → 10675, i.e. **23 BAMS of drift over 900 frames = 0.126° in 15 s = 0.51 °/min**, one revolution per ~712 minutes. Its base value is right too: `301.45 * -182.04 = -54885`, which as a `SWord` is `+10651`. <br>⚠️ **THAT DRIFT IS REALISTIC GYRO PRECESSION, NOT A PINWHEEL** — and I nearly called it the bug. Seeing a monotonic rise at constant heading looks exactly like a runaway until the RATE is computed; half a degree per minute is what a real gyro compass does. **Direction of change is not magnitude of change**, and only the second one distinguishes a defect from correct behaviour here. <br>**So the ground/air split is NOT visible in these values at all** — they are correct on the runway and correct at altitude. The PO's "works on the ground" must therefore be a difference in what is DRAWN, not in what is computed. <br>⭐ **The remaining gap is therefore how the MESH consumes those values** — the animation application that turns an anim byte/word into a rotated compass card — not the dial arithmetic and not the view. <br>⚠️ One observation to check rather than assume: `quickviewrec = &VRNorth` points the quick-view at a NORTH-referenced view record. If that leaves the camera world-referenced while the aircraft turns, the whole panel would swing relative to the pilot and a card could READ as spinning even with a correct value. **Distinguish "the card rotates within a still panel" from "the panel swings" before chasing the mesh animation** — they look similar in motion and have unrelated fixes. <br>_Superseded test (kept so it is not retried):_ **put the `AddTimedDial` at `MODEL.CPP:550` behind an env and A/B the panel.** ⚠️ **A gyro compass legitimately precesses** — if that timed dial IS the modelled drift, deleting it trades a spinning compass for a frozen one. **Measure the rate first:** a pinwheel and a slow precession are the same mechanism at different speeds. |
| PO-79 | **`.acmi` export is only ~20% of the dogfight, and jumps at the end** | **PO 2026-08-28.** The exported `.acmi` covered ~35 s of a dogfight roughly 5× longer, and **at the end both aircraft jump to being next to each other, displaced from where they had been manoeuvring.** Tacview video `/home/admin/Videos/260828_tacview_ma.mp4`; file `Videos/260828.acmi`. **MEASURED (this session):** `1024` time markers, `#0.00 → #51.15` at a uniform 0.05 s step, **2 objects**, 19 852 lines. ⭐ **1024 IS THE TELL — and it has happened before.** `ma_acmi.cpp`'s own S278 comment records an earlier incident where *"the working file held **1024 time markers (51 s)**"*. Two independent recordings ending at exactly 1024 = a **buffer/block boundary**, not a coincidence. ⭐ **The `.acmi` is exported FROM THE REPLAY, not recorded live** — `Replay.cpp:506` `ma_acmi_time((double)replayframecount / _hz)`. And the replay is **block-structured** (`FRAMESINBLOCK`, `StoreRealFrameCounts`, `thisblockendframe`, `currblock`). **Hypothesis, testable: the export walks ONE replay block, so it gets one block's worth of time AND the "jump" is the block boundary** — one mechanism explaining BOTH symptoms. ☑ **ROOT-CAUSED AND FIXED (2026-08-29).** The hypothesis below (the export *walks* one replay block) was the right neighbourhood and the wrong mechanism — the export **tees live** while recording. What resets is the CLOCK:

```c
ma_acmi_time((double)replayframecount / _hz);   // Replay.cpp:506 -- WITHIN-BLOCK counter
...
if (seconds <= g_acmi_lastT) return;            // ma_acmi.cpp -- non-increasing timestamps DROPPED
```

`replayframecount` runs `0..FRAMESINBLOCK-1` (**`FRAMESINBLOCK` is 1024**, `Replay.cpp:117`) and is reset when a block rolls. So the exported clock restarted every 51.2 s; every later marker was `<= lastT` and silently discarded — while the **object writes carried on**, unguarded.

**One mechanism, both symptoms:** exactly 1024 markers (`#0.00 → #51.15`) however long the sortie, AND every later position appended under the final `#51.15`, which Tacview draws as the aircraft leaping together. The PO's own file confirms it: **19,852 lines with only 1024 markers** — with 2 objects that is ~9,414 frames/object ≈ 470 s at 20 Hz, i.e. ~8 minutes of dogfight inside a 51-second timeline, matching "perhaps 20 %".

**Fix:** the export gets its own monotonic counter (`ma_acmi_next_frame`/`ma_acmi_reset_clock`), living in `ma_acmi.cpp` with the file it belongs to — a `static` in `Replay.cpp` would have started a second sortie's file wherever the first ended.

⚠️ **WHY THE L5 GATE PASSED THROUGHOUT.** `port/tacview_export.sh` assertion 5 checks the markers are MONOTONIC — and they always were, *by construction*, because `ma_acmi_time` drops the non-increasing ones. The gate could not fail on this defect. Its own header warned "the failure mode of this feature is a PLAUSIBLE FILE"; that warning was correct and insufficient. **New assertion 9 (completeness)**: samples-per-object must not far exceed the marker count. Control `CONTROL=blockclock` (`MA_ACMI_BLOCKCLOCK=1`) restores the old clock so the truncation reproduces on demand. ⚠️ Do NOT "fix" this by lengthening the ACMI writer — S278 already shows a truncation being fixed by a change that introduced a worse truncation, with the L5 gate green throughout because *structural validity is not completeness*. |


## 2026-08-29 — full suite green after the night's changes

`GATES: 23/23 clean` (`port/gates_all.sh`, all 23 including the two real-pointer gates added this
session), with `parity_2d` byte-identical on all five reference screens.

Regression cover for four source changes made this session:

| change | item |
|---|---|
| `Replay.cpp` — the `.acmi` export clock (`ma_acmi_next_frame`) | PO-79 |
| `Viewsel.cpp` — `MA_REVPADLOCK_AT` trigger + one-shot `backtrace()` at the reset site | PO-78 |
| `RLISTBXC.CPP` — opaque listbox fill, now `ma_lb_force_fill`-gated | PO-77 |
| `ma_olecontrol.cpp` — `MA_TRACE_LBID` + the force-fill hooks on three draw paths | PO-77 |

⚠️ The suite has run 23 gates since `real_mouse`/`real_hover` joined `$ALL` this session. The
previous full run ended `23 passed, 1 FAILED — <aborted-after-real_hover>`: not a product failure but
a stray-process race in the new gate, since fixed by reaping its child rather than signalling and
exiting. Worth remembering when comparing run totals — the "24" in that line counted an abort
marker, not a gate.

## PO-67 root cause (found, not yet fixed)

The compat `ShowWindow` records visibility only:

```c
BOOL ShowWindow(int nCmdShow) { m_maVisible = (nCmdShow != 0); return TRUE; }
```

`SW_SHOWMAXIMIZED` is therefore indistinguishable from "show", so `CMainFrame::OnGoBig()` — which
`LaunchFullPane` calls *specifically* to reach full size before building the panel — changes no
size. Everything downstream then measures a stale window: layout index 1 (800) chosen at
`window=800x600`, container rect 800x600, on a 1920x1080 canvas.

`MA_MAXIMIZE=1` (OFF by default) moves the frame **and** the view, since this port has no `WM_SIZE`
propagation. Title screen becomes correct — 1280x1024 art centred exactly, coverage 23% → 62.9%.
It is off because the **dialog** screens break at that size (labels overlap and double up). At
800x600 it is a proven no-op.

## Instruments

`MA_TRACE_PRESENT` (canvas coverage + painted bbox + centre pixel) · `MA_FORCE_RESINDEX` (force the
front-end layout) · `MA_TRACE_RES` (layout choice + container rect) · `MA_TRACE_REVPAD` ·
`MA_FORCE_PADLOCK` · `MA_SHOT` (2D canvas capture) · `BOB_KEYSEQ` (inject keys with modifiers) ·
`BOB_CLICKSEQ` (font-independent click recipes).

Reverts for recent changes: `MA_NO_REPLAY_SLOT_FIX`, `MA_NARROW_TBART`, `MA_NO_READ_OVERFLOW_FIX`,
`MA_NO_SMOKE_BOUND`, `MA_NO_REVPADLOCK`, `MA_CANVAS_ARTSIZE`.

## Cautions for whoever picks this up

- **Every 2D reference is 800x600.** No existing gate can judge a display-resolution layout change.
  `PARITY_RES=1080` exists but `port/ref/native1080` is intentionally empty — see its README.
- **A 1080 reference set would be a REGRESSION oracle, not a parity one.** `ref/native` came from
  the real game; there is no gold capture at 1920x1080 and there never will be.
- **MFC sources are case-colliding twins.** `Replay.cpp` vs `REPLAY.CPP` etc. Check `build.ninja`
  for which copy is compiled before editing, or the edit will compile clean and do nothing.
- **Treat every null result as a claim about the instrument** until a positive control says
  otherwise. Several wrong conclusions this session came from probes that were unreachable, not
  switched on, or measuring the wrong instant.


## PO-67 — dialog blocker fixed (2026-08-27)

The recorded next step was "fix DIALOG placement under a widened container". Reproducing it says
otherwise.

**Maximized, `prefs_3d` renders correctly** — tab bar, nine labels, nine combos, all cleanly placed.
Only `prefs_others` breaks, and the extra labels are identifiable: *Software Driver, Gamma
Correction, Lowest Frame Rate, Ground Shading, Reflections, Weather Effects* — the **3D tab's**
controls, still drawn under the Others tab's audio labels. So the defect is **stale controls
surviving a tab switch**, not layout scaling, and "widen the container" would not have touched it.

**But it is not a removal failure either.** With `MA_TRACE_SIZE=1`, both arms trace *identically*:

```
off  [hosted.remove] parent=0xb2e8850 removed=21 remaining=63
on   [hosted.remove] parent=0x99659f0 removed=21 remaining=63
```

One call, 21 removed, 63 left — the same either way. The stale controls exist at 800x600 too;
maximizing only makes them **visible**. Next step is therefore the DRAW pass, which composes
"parent-dialog screen origin + control client-relative pos": a changed parent origin is the suspect
for why the leftovers land somewhere visible only when the container grows.

| coverage | unmaximized | maximized |
|---|---|---|
| `prefs_3d` | 22.9 % | 63.7 % (clean) |
| `prefs_others` | 20.2 % | 51.0 % (doubled) |

### ⚠️ `MA_MAXIMIZE` was a presence test — fixed

`if (getenv("MA_MAXIMIZE"))` treated **`MA_MAXIMIZE=0` as ON**. That is the opposite of what "=0"
means to anyone reading it, and it silently invalidated the first A/B run here: both arms maximized,
0.00 % difference — which reads exactly like "the flag does nothing". Every other `MA_*` revert in
this port is spelled "=1 to enable / unset to disable", so the gate now honours `0`/`off`/`no`/
`false` as OFF. Verified:

| `MA_MAXIMIZE` | coverage | `[maximize]` fired |
|---|---|---|
| unset | 22.9 % | no |
| `0` | 22.9 % | no |
| `1` | 63.7 % | yes |


### The fix

`ma_gdi_clear_screen()` was called on **map→panel** (`_wasMap`) and **3D→panel** (`_was3d`) — the S155
comment gives the reasoning: *"wherever the panel does not cover, the stale frame shows through"*.
**Panel→panel was never covered.** At 800x600 each prefs tab's art lands on the same rect, so the
previous tab is overwritten and nobody saw it; maximized, the art lands differently and the previous
tab's TEXT survives underneath.

The leftovers are stale **pixels**, not stale controls — `MA_TRACE_GHOST` shows the same three owners
in both arms (`CMIGView`, `CSSound`, `RFullPanelDial`) with no ghost panel drawing, and
`ma_ole_remove_by_parent` removes an identical 21 controls either way. A panel teardown *is* a screen
transition, so the clear now happens there. `MA_NO_PANEL_CLEAR=1` reverts.

**Verified:** the Others tab maximized shows only its own ten labels. `parity_2d`: title, prefs_3d,
prefs_others, quickmission all **byte-identical**; `campaign_map` differs by **5184 px — exactly the
pre-existing S248 figure**, unchanged.

Also added: `MA_TRACE_GHOST_EVERY=<n>`. The ghost trace fired 1-in-200 passes, so a capture at frame
110 only ever showed pass 1 — the state *before* the dialogs are built, which is not the state under
investigation.


## S305 — the `.acmi` theatre is pinned (2026-08-27)

S296 shipped the Lon/Lat as an admitted guess and recorded the next step as *"dump a named
airfield's runtime World.X/Z"*. The dump was never needed. S296's reasoning was:

> the node tables name real places (Seoul, Pyongyang, Kimpo, Sinuiju) but carry UIDs, not
> coordinates -- the positions live in the world item data, reachable only at runtime.

True of the *runtime* positions, false of the *placements*. The world is built from
`SRC/BFIELDS/*.BFI`, checked-in text where every item carries an absolute `Posn { Abs { X, Z } }`
in world centimetres -- the same frame `Replay.cpp` samples as `_ac->World.X / 100`. The
coordinates were in the repo the whole time. **The blocker was an assumption about where data
lives, not missing data**, and it survived because "reachable only at runtime" was never tested.

Two landmarks from `MAINMIG.BFI`, two unknowns per axis (X is east, Z is north):

| | world X, Z (cm) | real |
|---|---|---|
| `UID_AfBlKimpo` | 61615953, 61326673 | 37.558 N, 126.791 E |
| `UID_AfRdSinuiju` | 41409080, 89304918 | 40.100 N, 124.400 E |

**Solution:** origin (0,0) = **31.986085 N, 119.500224 E**; **110063.9 m/°lat, 84512.2 m/°lon**.

The scale is the *map's*, not the Earth's, so it no longer derives from the reference latitude --
the old `111320 * cos(refLat)` form meant moving `MA_ACMI_REF` silently rescaled the theatre.

### Why this is a measurement and not two points joined by a line

Two points always fit a two-parameter model, so the fit itself proves nothing. What does:

- The solved latitude scale is **110064 m/° against a true 111132** -- within 1%. Nothing in the
  solve forced that; a mispaired landmark would have produced no such agreement.
- **Four airfields held out of the fit** all land within 6 km on an ~800 km theatre: Suwon 0.6,
  Pyongyang 2.6, Antung 3.7, Taegu 6.0. The residuals are about the size of the pairing ambiguity
  (Pyongyang and Taegu each have more than one field), so they bound the *game's* placement error.
- The pre-existing `ma-1v1-long.acmi` opens at U=412525 V=892952 -- Sinuiju's placement to 1.5 km.
  The quick mission does start over MiG Alley, confirming U/V share the BFI frame. That shared
  frame is the one assumption the whole solve rests on, and it is now checked rather than assumed.

### Gate

`port/acmi_georef.sh` drives the **real** `ma_acmi_object()` with the six airfield positions and
reads the Lon/Lat it actually writes -- not a re-implementation of the formula, which would pass
regardless of what the shipped code did. No GL, no flight. `CONTROL=1` restores the discarded
37.5N/127.0E guess and must turn all six rows red; it does, by ~900 km.

Existing recordings keep correct U/V, so `port/acmi_regeoref.py` recomputes their Lon/Lat in
place. `ma-1v1-long.acmi`: 131.671E 45.535N -> **124.381E 40.099N**, Sea of Japan -> Sinuiju.


## S313 — S248 answered by measuring the diff instead of asking

The row said: *"how many toolbar icons the campaign map shows before flying. Either the refs are
stale (rebase) or the guard is too broad (narrow)."* That is a question about a picture, and the
picture was already committed — the difference has been sitting in `parity_2d`'s output every run
since S248.

Clustering the 5184 differing pixels gives **exactly three connected components**, each a complete
icon cell:

| | bbox | size | px |
|---|---|---|---|
| toolbar button | x 286..333, y 52..99 | 48x48 | 2304 |
| toolbar button | x 748..795, y 52..99 | 48x48 | 2304 |
| filter button | x 624..647, y 28..51 | 24x24 | 576 |

2x2304 + 576 = 5184, and **nothing else differs** — no partial glyphs, no one-pixel shift, no
antialiasing fringe. Whatever this is, it is whole buttons appearing or not appearing.

Cropping the three cells from both images settles the direction: the **reference shows bare map**
(terrain, sea, terrain) and the **port shows toolbar buttons** (a star, a slashed rectangle, part of
a star). `ref/native` came from the real game, so the real game does not draw these three at this
pinned campaign state and the port does. **The guard is too broad.** No PO screenshot needed; the
choice between the two options was decidable from data already on disk.

**Leading suspect, not a conclusion.** `CRToolBar::OnGetFile` was widened from the old
`0x6800..0x7100` range to `filenum <= 0 || filenum > 0xFFFF` so that both WM_GETFILE handlers would
answer alike — a change made for good reasons (the narrow range was skipping 732 icons in one
session). Widening it supplies art to buttons that previously came back empty, and three of them
are ones gold leaves blank. That fits, but it is a hypothesis until the three FileNums are read.

**Next step:** capture campaign_map with the toolbar art trace on, read the FileNums for those three
slots, and find what separates them from the 615 that legitimately draw. `MA_NARROW_TBART=1` is the
A/B — if it removes exactly these three and nothing else, the mechanism is confirmed; if it removes
hundreds, the old range was never the right discriminator and the answer lies in the buttons' own
display condition, not in the art lookup at all.


## S314 — the S312 flip is reverted, and so is the refactor I tried to rescue it with

**S312 was wrong to ship, and the gates I chose are why I did not know.** parity_2d passed 5/5 at
1080 and 4-identical at 800; map_drag was lossless. All true, and all blind to the defect:
`port/panel_click.sh` is the gate that clicks what is drawn, and it reported

```
menu located at pixel (1100,470)
the menu was drawn at (1100,470) but clicking there did nothing
```

A front end that looks finished and ignores every click is the worst shape a UI regression can
take, and it shipped because I ran the gates that covered the change I had in mind rather than the
suite. I flagged the missing suite verdict in the S312 commit; flagging it is not the same as
having it.

Isolated by A/B on panel_click, since two of my changes were live at once:

| arm | result |
|---|---|
| `MA_MAXIMIZE=0` + panel offset **on** (S311 alone) | **PASS** |
| maximise **on** + panel offset off | **FAIL** |

So it is S312's flip, not S311's control placement. Traced: `listbox id=2063 rect=(810,370,105,100)`
against a click at (1100,470). The rect is 105x100 — far too small for a seven-item menu — so the
listbox's rect is computed from a different layout than the one used to draw it. **Pre-existing in
the maximise path; the flip only exposed it.** Default back to opt-in until it is fixed.

### The refactor that was supposed to help, and made it worse

S311 added the panel-art offset to the draw pass. Six sites compute that same origin, so I factored
them into one `hosted_origin()` — draw, hit-test, listbox click, scrollbar, and the `#id` resolver.
Structurally right, and it broke the 1080 gate: `prefs_others` and `campaign_map` went to 1.3M and
2.0M differing pixels, deterministically across three runs. The capture showed the **3D tab** where
the recipe asks for **Others** — the recipes navigate by clicking, and changing hit-testing changed
where their clicks land.

I then found a real asymmetry underneath (`ma_ole_click` omits the `h.type != CT_LISTBOX` term that
the draw pass and the `#id` resolver both carry, so a game-positioned listbox's hit rect sits
`parent->m_maX` from where it is painted) and aligned it. **That did not fix the 1080 arm either** —
recorded because it is a real inconsistency worth fixing later, and because "I found a plausible
asymmetry" is not the same as "I found the cause".

Reverted to the S311 state the 1080 references were blessed against. Re-blessing was not an option:
the new captures are the *wrong screens*, so committing them would enshrine broken navigation as
the expected result — the exact failure `ref/native1080`'s own README was written to prevent.

**Verified after the revert:** parity_2d 1080 **5/5 byte-identical**, parity_2d 800 four identical
with campaign_map at its unchanged 5184 px, panel_click **PASS**.


## S315 — S248 closed: loading a file is not the same as being meant to draw it

S313 measured that campaign_map's 5184 px is exactly **three whole icon cells**, and that the
gold-derived reference shows **bare map** where the port draws buttons. S315 finishes it.

**The guard is the cause, measured not argued.** `MA_NARROW_TBART=1` — the old
`0x6800..0x7100` range — makes campaign_map **byte-identical** (0 px of 480000). The current wide
guard gives 5184. So the widening is responsible, and it changes *only* those three cells.

**The offenders are two files, not three.** `MA_TRACE_TBART_DELTA=1` logs exactly the FileNums the
wide guard accepts and the narrow one rejects, on this screen: **`0x6607` and `0x660c`**. Two art
files for three cells fits what the crops show — the same star icon drawn twice, at 48x48 and
24x24, plus one slashed icon.

### The reasoning error the widening was built on

The widening's own justification is in the source:

> dir 102 is NOT unloaded: `MA_PROBE_FILENUM` asks the game's own file system and gets
> `0x660c size=3382 data=yes` … The guard was excluding a directory that loads fine.

Every word of that is true, and it does not support the conclusion. **That a file loads does not
mean the button is meant to draw it.** The file system answers "can this be read"; only the gold
capture answers "should this appear". `0x660c` — the very file cited as proof — is one of the two
that should not be drawn.

### The fix, and why it is not a revert

The widening genuinely recovered real icons (615 OK vs 732 skipped in one session), so reverting it
wholesale would trade this defect for that one. Instead the 0x66xx directory alone is re-excluded
and everything else keeps the wide guard.

| gate | result |
|---|---|
| `parity_2d` (all 5) | **5/5 byte-identical** — first time |
| `map_filter` | PASS |
| `map_icon_click` | PASS |
| `help_click` | PASS |

The three icon-dependent gates confirm the recovered icons are still there, which is the property
that would break if the exclusion were too broad.

### PO-80 (S326 follow-on, 2026-08-28) — real_hover.sh is SELF-REFERENTIAL and cannot see an origin error

`port/real_hover.sh` derives its probe points from the rect the port REPORTS (`[hover] org=…`), then
asserts the rows it lights are self-consistent. That can never fail on an origin bug: move the
origin and the gate moves its probes with it.

Measured, both arms of the S326 control, same listbox (105x100):

| arm | reported origin |
|---|---|
| fixed (`ma_ole_origin`)   | (1130,398) — and a second rect at (810,370) |
| control (`MA_NO_HOVERORG=1`) | (810,370) only |

The fix demonstrably MOVES the origin by (320,28) — the exact panel offset S326 named — but the
gate passes both ways, so it cannot certify the fix.

**What is actually needed:** an INDEPENDENT source of truth for where the listbox is DRAWN.
`ma_ole_draw_all` already records `drawOx/drawOy` per hosted control; the assertion should be
`hit-test origin == draw origin`, which is unfalsifiable-by-construction no longer.

⚠️ This also covers the PO's symptom properly. The report was VISUAL ("hovering causes the wrong
menu item to highlight"). Every assertion in the current gate is on the HIT-TEST ROW; a highlight
DRAWN in the wrong place would pass it untouched. Until this is done, S326 must be described as
"the hit test is self-consistent and the origin moves as intended", NOT as "the PO's bug is fixed".

**Done when** the gate compares hit-test origin against draw origin, the control (`MA_NO_HOVERORG=1`)
FAILS, and the pass arm is green on every rect the sweep finds.

☑ **CLOSED (2026-08-29).** Both arms behave, and S326 is confirmed to fix a real defect:

```
PASS arm      rect (1130,398) 105x100
  0. hit-test origin == DRAW origin   yes (1130,398)
  1..4                                yes        -> PASS
CONTROL arm   rect (810,370) 105x100
  0. hit-test origin == DRAW origin   NO  <-- hit=(810,370) drawn=(1130,398)
     the highlight tracks a point -320,-28 px from the cursor   -> PASS(control)
```

The (320,28) offset is exactly the panel origin S326 named, and it is the PO's report as a number:
the listbox is PAINTED at (1130,398) and was HIT-TESTED at (810,370), so hovering the visible menu
lit a different item.

Three things had to be fixed before the gate could say anything:
1. **It was self-referential.** Probe points were derived from the hit-test origin, so it stayed
   consistent wherever that origin was. `drawOx/drawOy` are recorded by PAINT (S84: "store what
   paint did, never re-derive it"), which is the one fact the hit test cannot talk itself into
   agreeing with.
2. **The control pulled the wrong lever.** `MA_NO_DRAWORG=1` only selects a different branch INSIDE
   `ma_ole_origin`, whose fallback still resolves correctly. `MA_NO_HOVERORG=1` restores the exact
   pre-S326 line. A control must disable the fix under test, not something adjacent to it.
3. **It probed a phantom.** `drawOx` is -1 until the first paint, so the sweep briefly sees the
   control at its raw origin too. The gate failed on that unpainted rect — a FAIL on something no
   user can hover. Unpainted rects are now skipped: what was never painted was never on screen.
