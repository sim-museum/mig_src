# MiG Alley native Linux port — session status (2026-06-23)

Branch: `linux-port` · Remote: `git@github.com:sim-museum/mig_src.git`

## Summary

The game remains a playable native Linux (SDL2, 32-bit) build — title → menus → Quick
Mission / Campaign → 3D flight, with audio, keyboard flight controls, and settings
persistence. This session ran scrum Sprints 10-14, delivering: a validated joystick fix,
a new hosted control type (REdit), and a now **functionally-complete loadgame screen**
(select a save → Load → the campaign operational map of Korea loads).

## This session's work (commits, oldest → newest)

| Commit | Sprint | What |
|--------|--------|------|
| `657e94a` | **S10 — M7 input** | Joystick LIVE-validated; fixed a real axis-mapping bug. |
| `00fd28f` | **S11 — M6 UI** | Host the REdit (CREditCtrl) OCX control. |
| `ae2234d`+3 | **S12 — M6 UI** | Correct a wrong "loadgame doesn't render" finding; localize the file-list gap. |
| `bebf094` | **S13 — M6 UI** | loadgame file list shows save games ("Auto Save"). |
| `fdbcd9c` | **S14 — M6 UI** | loadgame file-select → Load loads the save → campaign map. |

### S10 — joystick flight input (LIVE-VALIDATED + real bug fixed)
The Sprint-9 impediment (a concurrent BoB session holding the stick) was cleared, so the
shipped SDL→DirectInput bridge was validated end-to-end — and validation **caught a real
bug**: all six DirectInput axis GUIDs (`GUID_XAxis`…`GUID_RzAxis`) were defined as the same
all-zero `BOBGUID`, so the game's classifier saw every axis as X — roll drove rudder,
throttle drove aileron, the stick axes never mapped. Fix: distinct real axis GUIDs
(`bob_stubs.cpp`) + correct per-SDL-axis order (`bob_video.cpp`, `X,Y,Rz,Z` for the Extreme
3D Pro). New `BOB_AUTOJOY` scripted-deflection injector proves the chain: roll sweep →
`axisvalues.aileron` ±29000 (others isolated); roll-hold A/B frames show the aircraft banks.
Stress 8/8.

### S11 — REdit (CREditCtrl) OCX hosting
The text-entry control (load/save savename field, visitorsbook, variant name) is now a
first-class hosted type: `ma_oleedit.cpp` + `CT_EDIT` router + `oleedit` build mode. Two
reusable compat fixes uncovered: stubbed `ON_WM_GETDLGCODE()` (the one un-stubbed message-map
macro, which leaked from CREditCtrl's map) and the `DLGC_*` flags.

### S12 — loadgame finding corrected + file-list gap localized
Sprint 11 had wrongly concluded the loadgame screen "doesn't present" — a background
misidentification (`loadsave.bmp`, a pilots-by-truck photo, mistaken for the title; the real
title is the blue "Mig Alley" jet splash). **The loadgame screen does render** (background +
CLoad controls + the hosted REdit field). Localized the real gap to the file-list rows.
(Several follow-up "never draws / not registered" conclusions were trace-cap artifacts — the
title menu listbox exhausted the static-counter trace caps before navigation; lesson: gate
per-frame-control traces on identity, not a count.)

### S13 — file list renders save names ("Auto Save")
Root cause: the row was drawn at y=-rowheight (scrolled off the top, clipped). `m_lVertScrollPos`
was a bogus 32 for a 1-row list because `UpdateScrollBar()` runs at populate time
(`MakeFileList`) but the port sizes the listbox client only at draw time — so `GetClientRect`
returned a zero size and the scroll clamped to a fake maxvalue. Fix (`MA_LINUX`):
`CRListBoxCtrl::OnDraw` re-clamps `m_lVertScrollPos` to the valid range from the real draw-time
`rcBounds`. The file list now shows "Auto Save". No regression.

### S14 — file-select → Load loads the save (→ campaign map)
Clicks on the CLoad file list went nowhere — the idle click handler only hit-tested the
FullPanelDial menu listbox, so `CLoad::OnSelectRlistboxfile` never ran and `selectedfile`
stayed empty → `DoLoadGame` bailed. Fix: new `ma_ole_listbox_click` routes a click on a
child-dialog listbox (id≠0) to its Select handler via the RTTI eventsink; `CLoad::filename` is
a reference to `selectedfile`, so the pick propagates; the Back/Load menu was already wired.
Validated: file-click → `evt_fire CLoad Select` → Load → `DoLoadGame` →
`CFiling::LoadGame("Auto Save.sav") → 1` → the **Korean-peninsula operational map renders**.
Crash-free; 3D path stress 4/4; Preferences combos unaffected.

## Diagnostics (gated env vars, default off)
New this session: `BOB_AUTOJOY=roll|pitch|sweep|center|left|right|up|down`, `MA_TRACE_JOY`
(now also `[joy/axisvalues]`), `MA_TRACE_EDIT`, `MA_TRACE_LIST` (listbox render suite),
`MA_TRACE_OLE` `[DoLoadGame]`, `MA_TRACE_CLICK` `[click] file-listbox`. Plus existing
`MA_ENABLE_3D/MAP`, `MA_TRACE_3D/DD/FILL/KEY/SKY/FIND`, `BOB_DUMP_FRAME`/`MA_DUMP_BACK`,
`BOB_EXIT_AFTER_DUMP`, `BOB_CLICKSEQ`, `BOB_DRIVE_C`.

## Build / run
```
g++ -m32 -no-pie port/build/{obj,obj2,objmfc,objmfc2,objole}/*.o \
  -Wl,--allow-multiple-definition -lSDL2 -lGL -lopenal -lfluidsynth -lpthread -lm -o wmig
BOB_RUN_INIT=1 BOB_DRIVE_C=<wine drive_c> ./wmig                                   # title → menus
# loadgame: title→Load Game, click a save, click Load → campaign map:
BOB_RUN_INIT=1 MA_ENABLE_MAP=1 BOB_DRIVE_C=… \
  BOB_CLICKSEQ="40,588,260;95,80,112;150,75,567" ./wmig
```

## Next (Sprint 15 candidates)
- Front-end: credits / replay screens (remaining title sub-dialogs), reusing the OCX/host infra.
- 3D fidelity: M4/M8 operational-map tile **colour fidelity** (greyish vs Wine) — now reachable
  since load→map works.
- Backlog: campaign save/resume E2E; broader 3D fidelity A/B vs Wine. (Smacker video cut.)
